# BuildSelf 开发日志 — 2026-08-24

> 个人成长 App（Flutter · Android/iOS 双端 · 本地优先）
> 今日 1 次提交，聚焦：首页「今日待办」已完成项展示逻辑修正 + 预览条数调整。

---

## 一、提交记录

| Commit | 说明 | 规模 |
| --- | --- | --- |
| `43591f9` | 今日待办已完成项仅保留当天展示（今日 0 点 ~ 23:59:59），跨天自动消失；预览条数调整 | 2 文件 +19/-4 |

当前分支 `feature/ui`，相对 `origin/main` 领先 6 个提交（含今日本次），工作区干净。

---

## 二、改动明细

### 1. 首页「今日待办」已完成项跨天残留修复
- **问题**：原实现拉取的是「所有已完成的待办」（按完成时间倒序取前 2 条补位），未按天过滤，导致**昨天甚至更早完成的待办，第二天依然显示在今日待办列表中**。
- **修复**：已完成补位仅取「今日 0 点之后完成」的记录（`completed_at >= 今日 0 点`），跨天后自动从首页消失，符合「已完成项保留时长只限今天（23:59:59 前）」的预期。
- **实现**：`TodoRepository.getAll` 新增可选参数 `completedAfter`（仅 `completed == true` 时生效），在 SQL 层过滤 `completed_at`（ISO8601 字符串字典序与时间序一致，可直接比较），避免内存拉全量再过滤。

### 2. 首页待办预览条数调整
| 项 | 调整前 | 调整后 |
| --- | --- | --- |
| 未完成待办（优先展示） | 最多 4 条 | **最多 8 条** |
| 今日已完成（删除线补位） | 最多 2 条 | **最多 4 条** |

- 同时修正 `_buildTodoContent` 处过时注释（原写「最多 3 条」，与实际不符）。

### 3. 数据卡「待办」数量核对
- 首页数据条「待办」卡显示的是 `TodoRepository.getActiveCount`（`is_completed = 0` 的计数），**本身就是「所有未完成待办的数量」**，符合预期，本次未改动。

---

## 三、质量校验

- `dart analyze lib` → **0 error**（140 条 warning/info 均为项目既有风格基线：unused_import / prefer_const / withOpacity 弃用等，本次改动 0 新增）
- 真机验证通过后提交（完成项删除线样式、跨天消失、条数上限均确认）

---

## 四、环境备注

- Windows 下 `flutter analyze` 全量扫描会命中 `tools/` 内嵌 Flutter SDK 源码（27 万条噪音），验证请用 `/d/flutter/bin/cache/dart-sdk/bin/dart.exe analyze lib`（`/d/flutter/bin/dart` 无扩展名 wrapper 在 Git Bash 下不可用）。
- 沙箱环境对 `.git/refs/` 下新文件写入存在「假成功」拦截（commit 对象与 reflog 正常、ref 文件未落盘，`git log` 报 no commits yet）。修复：沙箱外 `mkdir -p .git/refs/heads/feature && printf '<sha>\n' > .git/refs/heads/feature/ui`。后续提交后若 `git log` 异常，先检查 `ls .git/refs/heads/`。

---

## 五、遗留事项 / 后续建议

- 首页「今日复盘」仍为空态占位（数据模型未建）
- 目标/阅读的统计概览小图标仍是 Material（emoji 规范未覆盖短/中/长/成就语义）
- 习惯打卡暂无周视图/历史日历；番茄钟模块尚未实现
- 待办删除为物理删除（未接入回收站），如需可后续调整
