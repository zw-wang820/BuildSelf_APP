# 2026-09-04 开发日志

## 补推 09-03 KISS 复盘两笔提交（09-04 午后）
- 发现昨日 push 实际未生效（远端仍停在 `fb52cab`，本地 `daea678` 领先 2 笔：`8512583` KISS 复盘模块 + `daea678` 开发日志）。
- 已补推成功：`fb52cab..daea678 feature/ui -> feature/ui`，远端与本地同步。
- **教训**：push 后必须 ls-remote 校验，勿只看命令退出码/空输出。

## main 强制合并 feature/ui（09-04 15:15-15:17）
- 用户拍板：远端 main 唯一提交 `02c713e`（"手机可运行的稳定版本"，孤儿 root commit）无保留价值。
- **核心发现**：远端 main 的 `02c713e` 与 feature/ui 根 `e3fa0319` 是**两个 hash 不同但 tree 完全相同**的 root commit（典型 sandbox ref 落盘坑的连锁事故），merge-base 为空、`02c713e` 不是 feature/ui 的祖先。
- **预检**：`git tag --points-at 02c713e` 空（孤儿无 tag/release 引用，丢弃零损失）；`gh` 不可用（无 gh 认证，改用 push 失败信息兜底判断保护规则）；SHA 对齐（main=`02c713e` / feature/ui=`daea678` / develop=`02c713e`）。
- **沙箱外执行**：
  1. 建 backup ref `backup/main-orphan-20260904`（printf 兜底落盘指向 `02c713e`）
  2. `git branch -f main origin/feature/ui`（本地 main 指针移到 feature/ui HEAD）
  3. `git push origin main:refs/heads/main --force-with-lease`（SSH 443）
- **推送结果**：远端 main 强制从 `02c713e` 更新到 `daea678`（38 个新提交全部进入 main）；远端无 branch protection，一次通过。
- **8 项校验全过**：ls-remote=daea678 / ref 文件落盘=daea678（mtime 15:17）/ fetch 后仍=daea678 / 远端 main 39 提交 / feature/ui 未受影响 / develop 未受影响 / branch -vv 干净 / status 干净。
- **后续修复**：本地 main upstream 被 git 自动设为 `origin/feature/ui`（因 HEAD 相同），后续 `git branch --set-upstream-to=origin/main main` 修正。
- **当前拓扑**：main == feature/ui == `daea678`；develop 仍为 `02c713e`；`backup/main-orphan-20260904` 留底。

## docs/ 散落 markdown 归档整理（09-04 15:30-15:43）
- 用户决定：把 22 个根目录 markdown 归档到 `docs/` 下分类子目录。
- **方案 B（采用）**：4 个中文子目录
  - `docs/项目元信息/` (3)：Buildself_PRD、CODE_WIKI、修改记录
  - `docs/每日开发日志/` (7)：20260823~20260903 七份开发日志
  - `docs/模块交互说明/` (9)：工作/待办/目标/习惯/书籍/自定义分类 交互说明
  - `docs/资料与方案/` (1)：阅读指南
- **保留原位**：`README.md`（GitHub 仓库入口）+ `ios/Runner/Assets.xcassets/LaunchImage.imageset/README.md`（Xcode 资源标识，禁止移动）。
- **顺手修复**：`Buildself_PRD.md.md` 历史命名 bug → `Buildself_PRD.md`。
- **操作**：`git mv` 21 个文件（19 移动 + 1 rename + 1 新建子目录），保留文件级 blame 历史。
- **dart analyze 校验**：`/d/flutter/bin/cache/dart-sdk/bin/dart.exe analyze lib` → 0 error / 0 warning，129 info（全部历史存量：withOpacity/activeColor/textScaleFactor deprecated + prefer_const_* 建议 + 1 个 use_build_context_synchronously），与本次纯文档移动零关系。
- **commit `2aa8694`**：`chore(docs): 归档散落 markdown 到 docs/ 分类目录`，20 files changed, 0 insertions(+), 0 deletions(-)（rename-only）。
- **推送**：`daea678..2aa8694 feature/ui -> feature/ui`（SSH 443）。
- **沙箱 ref 落盘坑又复发**：commit 后 `.git/refs/heads/feature/ui` 未落盘。第一次 printf 兜底踩了新坑——`git rev-parse HEAD` 当时已坏（HEAD ref 解析失败），`$full_sha` 变量是空，bash 把变量名 `"HEAD"` 作为字面字符串写入 ref 文件，把 ref 写坏，`git status` 直接报"No commits yet"。**新教训**：printf 兜底时不能用 `git rev-parse HEAD` 拿 SHA（HEAD 可能已坏），要直接用 `cat .git/logs/HEAD` 或 `grep <new-commit-sha> .git/logs/HEAD` 从 reflog 拿 SHA。

## 未来 docs/ 新增约定（固化）
- 每日 commit 后的开发日志 → `docs/每日开发日志/`
- 新模块的交互说明 → `docs/模块交互说明/`
- 新调研/方案 → `docs/资料与方案/`
- 项目级元信息 → `docs/项目元信息/`
- 上述都不是 → 临时放根目录，提交前 review 决定归位
