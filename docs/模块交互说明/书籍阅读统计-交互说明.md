# 书籍阅读统计 - 交互说明

> 版本：v0.1（待审核）  
> 范围：单本书的笔记构成统计（摘抄 / 心得 / 思考 / 改变 各多少条）  
> 前置：阅读模块已完成 书籍编辑、笔记编辑/时间显示、封面改造（均已上线）

---

## 一、需求理解

用户想对**某一本书**查看阅读产出统计：这本书记了多少条笔记，其中摘抄 / 心得 / 思考 / 改变各占多少。这是一个**轻量查看型**需求（看数字），不是复杂分析型（不需要趋势、图表）。

---

## 二、推荐方案（A：详情页统计弹层）

### 入口

书籍详情页 → 顶部书籍信息卡**右侧**新增「📊 统计」按钮 → 点击从底部弹出统计面板。

```
┌────────────────────────────────────┐
│ ← 书名                    ⋮ 菜单   │  ← AppBar（不变）
│ ┌────────┬──────────────────────┐  │
│ │ 📚封面 │ 书名 / 作者            │  │
│ │        │ [想读] 状态徽章  📊统计 │  │  ← 信息卡新增统计按钮
│ └────────┴──────────────────────┘  │
│ [全部][摘抄][心得][思考][改变]      │  ← TabBar（不变）
│         笔记列表（不变）            │
└────────────────────────────────────┘
        ▲ 点击「📊 统计」弹出
┌────────────────────────────────────┐
│ 阅读统计 —《书名》             ✕   │
│ 笔记总数 12 条                      │
│ ┌──────────────────────────────┐  │
│ │ 📝 摘抄       5 条  ▓▓▓░ 42% │  │
│ │ 💡 心得       4 条  ▓▓░░ 33% │  │
│ │ 🤔 思考       2 条  ▓░░░ 17% │  │
│ │ 🔄 改变       1 条  ░░░░  8% │  │
│ └──────────────────────────────┘  │
│ 近 7 天新增：3 条                    │
│ （点任意一行 → 收起面板，切换到     │
│   详情页对应笔记 Tab）              │
└────────────────────────────────────┘
```

### 交互细节

| 环节  | 行为                                                                            |
| --- | ----------------------------------------------------------------------------- |
| 打开  | 点击「📊 统计」→ 底部弹出面板（`showModalBottomSheet`，圆角 + 拖拽收起），异步加载统计（加载中显示转圈）           |
| 数据  | 一次性取该书全部笔记（现有 `getNotesByBook`），内存按类型分组计数，**无新表、无新接口**                        |
| 展示  | 4 行统计：类型 emoji + 名称 + 条数 + 占比进度条；顶部显示笔记总数；底部显示「近 7 天新增 N 条」（按 `createdAt` 归日） |
| 下钻  | 点击某一行 → 收起面板 → 详情页自动切换到对应笔记 Tab（如点「摘抄」→ 切到摘抄 Tab），快速查看该类笔记                    |
| 空状态 | 笔记总数为 0 时：面板显示「这本书还没有笔记」+ 引导「去添加第一条笔记」（点击收起并切到全部 Tab）                         |
| 刷新  | 面板每次打开都重新取数；新建/删除笔记后再次打开即为最新                                                  |
| 关闭  | 下滑 / 点击 ✕ / 点空白处关闭                                                            |

### 为什么选弹层而非独立页

- 统计是**查看数字**的轻动作，弹层不离开当前页面、上下文不打断
- 详情页本身就是该书的聚合页，统计挂在这里语义最自然
- 下钻回 Tab 顺滑（面板 → 列表同页完成）
- 独立页为将来扩展（趋势图/多维度）留了空间，但现在做属于过度设计

---

## 三、备选方案对比

| 方案           | 形态                   | 优点           | 缺点                   | 建议                  |
| ------------ | -------------------- | ------------ | -------------------- | ------------------- |
| **A 弹层**（推荐） | 底部统计面板 + 下钻 Tab      | 轻量、不打断、实现成本低 | 不适合复杂分析              | ✅ 当前需求              |
| B 独立统计页      | 新路由 `/reading/stats` | 可扩展趋势/图表     | 多一层页面，空数据时显重         | 若后续要「阅读时长/多本书对比」再升级 |
| C 信息卡内嵌统计行   | 详情页信息卡下方直接 4 行数字     | 零点击直达        | 与下方笔记 Tab 语义重复、信息卡变重 | 不推荐                 |

---

## 四、数据与实现要点

1. **无迁移**：复用 `ReadingNote.noteType`（4 类，`enum` 已带中文 label）与 `createdAt`。
2. **统计逻辑**：`getNotesByBook(bookId)` 全量 → `notes.where((n) => n.noteType == X).length` × 4 + 总数；近 7 天按 `createdAt.isAfter(now - 7d)` 计数。
3. **下钻联动**：面板通过回调把选中的 `NoteType?` 传回详情页 → `_tabController.animateTo(对应 index)`（全部=0、摘抄=1、心得=2、思考=3、改变=4，与现有 `_tabFilters` 顺序一致）。
4. **UI 复用**：进度条用占比（条数/总数），颜色沿用各类型场景色（摘抄=reading 紫、心得=info 蓝、思考=warning 橙、改变=success 绿），样式对齐书架统计卡。
5. **改动文件**：`book_detail_screen.dart`（入口按钮 + 弹层组件 + 下钻联动），约 150 行；不动数据层。

---

## 五、验证清单（真机）

1. 详情页信息卡出现「📊 统计」按钮，点击弹出面板
2. 4 类数量与笔记列表实际一致；总数为 4 类之和；占比进度条总和 ≈ 100%
3. 「近 7 天新增」与最近 7 天新建的笔记数一致
4. 点「摘抄」行 → 面板收起 → 详情页切到摘抄 Tab
5. 新建 1 条笔记后重新打开面板 → 数字 +1
6. 删除笔记后重开 → 数字 -1
7. 无笔记的书：面板显示空状态引导

---

## 六、待确认

- [x] 方案 A（弹层）是否采纳？
- [x] 是否需要「近 7 天新增」这一附加维度？（默认加，可去掉）
- [x] 4 类行是否都支持点击下钻？（默认支持）
