# 待办回收站 — 交互说明文档

> 功能：待办删除从「物理删除」改为「软删除进回收站」，支持恢复 / 永久删除 / 30 天自动清理  
> 状态：**方案待确认** ｜ 日期：2026-08-25 ｜ 分支：feature/ui

---

## 一、功能概述

当前待办左滑删除是**物理删除**（`DELETE FROM`，无法恢复），回收站只覆盖工作/生活/目标/碎碎念四类记录。本次将**待办纳入回收站体系**：

- 删除待办 → 移入回收站（软删除），可恢复
- 回收站展示待办条目，支持恢复 / 永久删除
- 超过 30 天自动清理（与现有回收站规则一致）
- 回收站中的待办**不参与**任何列表 / 统计 / 计数

## 二、改动范围

| 层     | 改动                                                                                                                                                     |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 数据库   | v5 → **v6 迁移**：`todos` 表新增 `deleted_at`、`updated_at` 两列（兼容现有通用 `softDelete`/`restore` 方法，它们会写 `updated_at`）                                            |
| 数据层   | `TodoRepository.delete` 改为**软删除**（写 `deleted_at`，不物理删）；`getAll` / `getActiveCount` / `getStats` / `ensureDueInstances` 全部排除已删除记录（`deleted_at IS NULL`） |
| 清理    | `purgeExpiredTrash` 增加清理 `todos`（> 30 天物理删除）                                                                                                           |
| 回收站页  | 接入 `todos`：查询 `deleted_at IS NOT NULL`，显示待办条目，恢复 / 永久删除复用现有能力                                                                                          |
| 待办列表页 | 删除成功 Toast 文案改为「🗑️ 已移至回收站」                                                                                                                            |

## 三、交互流程

### 删除（列表页）

```
左滑待办 → 滑到底 → 弹出确认框（现有二次确认逻辑不变）
  ├─ 取消 → 卡片弹回
  └─ 确认删除 → 条目从列表消失 + Toast「🗑️ 已移至回收站」
                （实际为软删除，数据进回收站，可恢复）
```

### 回收站（设置 → 数据备份 → 回收站）

```
回收站列表新增「待办」条目，与其他模块卡片样式一致：
  ┌─────────────────────────────┐
  │ TODO               已删 08.25 │   ← 模块徽章（todo 场景色）+ 删除日期
  │ 准备周报数据                  │   ← 待办内容
  │ [↺ 恢复]  [🗑️ 永久删除]      │
  └─────────────────────────────┘
  ├─ 恢复 → 待办回到列表页（未完成状态保留原样），Toast「已恢复」
  └─ 永久删除 → 二次确认后物理删除，Toast「已永久删除」
```

- 待办条目 subtitle：显示分类（💼 工作 / 🌿 生活 / 📖 阅读 / 📚 学习），无内容留空
- 清空回收站：一并清空待办（现有"清空"按钮逻辑已遍历全部条目，自动覆盖）
- 空态文案不变（"删除的记录将保留 30 天后自动清理"，现在对待办也生效了）

## 四、统计口径影响

- 软删除后：回收站中的待办**不计入**首页待办预览、数据卡"待办"计数、待办统计页（`getStats` 已排除）
- 重复待办：已删除的重复模板/实例不再触发 `ensureDueInstances` 补建

## 五、边界与约束

1. 已删除待办恢复后：`is_completed`、`due_date`、`completed_at`、重复规则等字段原样保留
2. 删除的**重复实例**进回收站后，其所属模板不再为其补建新实例（`ensureDueInstances` 排除已删除）
3. 30 天自动清理为物理删除，清理后不可恢复（与现有规则一致）
4. 首页「已完成仅保留当天展示」规则不受影响（软删除与展示规则独立）

## 六、技术实现要点

- **v6 迁移**（`_onUpgrade`，`oldVersion < 6`）：
  ```sql
  ALTER TABLE todos ADD COLUMN deleted_at TEXT;
  ALTER TABLE todos ADD COLUMN updated_at TEXT;
  ```
  `AppConstants.databaseVersion` 5 → 6
- `TodoRepository.delete`：改为 `_db.softDelete(AppTables.todos, id)`
- 查询排除：`getAll` / `getActiveCount` / `getStats`（两处）/ `ensureDueInstances` 的 `where` 增加 `deleted_at IS NULL`
- `purgeExpiredTrash`：增加
  ```dart
  await db.delete(AppTables.todos,
      where: 'deleted_at IS NOT NULL AND deleted_at < ?',
      whereArgs: [purgeBefore.toIso8601String()]);
  ```
- `TrashScreen._loadTrash`：增加 todos 查询（title = content，subtitle = category.label，moduleTag `TODO`，moduleColor `AppColors.todo`）

## 七、验收清单

- [x] 左滑删除待办 → 二次确认 → 列表消失 + Toast「已移至回收站」
- [x] 回收站出现该待办（TODO 徽章 + 内容 + 删除日期）
- [x] 恢复后待办回到列表页，状态/截止/重复规则原样
- [x] 永久删除 → 二次确认 → 物理删除，回收站与列表均不可见
- [x] 清空回收站包含待办
- [x] 回收站中的待办不影响首页预览、数据卡计数、统计页数字
- [x] 30 天自动清理包含 todos（代码层）
- [x] 删除的重复模板不再补建新实例
- [x] `dart analyze lib` 0 error；真机验证通过
