# BuildSelf 开发日志 — 2026-08-26

> 个人成长 App（Flutter · Android/iOS 双端 · 本地优先）
> 今日共 5 次提交，覆盖：待办截止时间修复与排序统一、待办自定义分类、书架 FAB 遮挡修复、读书笔记编辑与时间显示。

---

## 一、提交记录

| Commit | 说明 | 规模 |
| --- | --- | --- |
| `58b0655` | 待办截止时间动态刷新 + 编辑防篡改 + 编辑表单显示创建时间 | 2 文件 +51/-6 |
| `dd78fe5` | 待办清单页排序统一 + 排序逻辑抽公共方法 | 3 文件 +41/-24 |
| `3a1c147` | 待办自定义分类：内置四类 + 用户自定义（新增/重命名/删除） | 16 文件 +1159/-113 |
| `f4e48d6` | 书架 FAB 抬升到底栏上方，修复 tab 入口按钮被遮挡 | 1 文件 +3 |
| `9b589b5` | 读书笔记可点击编辑 + 列表/编辑页时间显示 | 3 文件 +107/-11 |

当前分支 `feature/ui`，相对 `origin/feature/ui` 领先 5 个提交，工作区干净。

---

## 二、模块改动明细

### 1. 待办截止时间动态刷新（fix，`58b0655`）
- **根因**：`Todo.dueLabel` 对相对类型（今天/明天/后天）返回固定文案，不随日期变化；`dueDate` 字段本身存的是正确的绝对日期。
- **修复**：`dueLabel` 改为基于 `dueDate` 与今天的天数差动态计算（0 天=今天、1 天=明天、-1 天=昨天、其余 MM/DD）。
- **同源次生 bug**：编辑表单保存时按当天重算相对截止，会把"昨天到期"的待办悄悄改成今天（数据篡改）。修复：编辑模式下相对截止不再匹配时表单回退为「自定义」显示原日期，保存不重算。
- **增强**：编辑表单标题下方显示「🕐 创建于 …」（今天/昨天 HH:mm；同年 MM/DD HH:mm；跨年 YYYY/MM/DD HH:mm），让用户理解相对截止的锚点。

### 2. 待办清单页排序统一（refactor，`dd78fe5`）
- 排序逻辑抽到 `Todo` 模型公共方法：`compareActive`（未完成段：截止升序、无截止排最后、优先级高→低）与 `compareForList`（混合：未完成优先 + 已完成按完成时间倒序）。
- 首页「今日待办」与清单页排序从此一致。

### 3. 待办自定义分类（feat，`3a1c147`）
- **数据库 v7 迁移**：新增 `todo_categories` 表（内置四类工作/生活/阅读/学习 + 用户自定义）。
- `Todo.category` 改 String，存量数据兼容解析；新增 `TodoCategoryInfo` 模型 + 分类解析器。
- 表单分类区：内置 + 自定义 + 新建弹窗（重名校验）；列表筛选动态 tab；分类管理页（重命名同步/删除归 work/内置只读）；统计与回收站适配。

### 4. 书架 FAB 被底栏遮挡（fix，`f4e48d6`）
- **用户反馈**：底部 tab 进「阅读」看不到 + 按钮，首页「正在阅读」卡进入能看到，两入口不一致。
- **根因**：`BookshelfScreen` 同时作底部 tab 常驻页（`IndexedStack` + `extendBody: true`）与 push 全屏页。作 tab 时 FAB 默认 `endFloat` 沉底，被毛玻璃底栏盖住；push 全屏时正常显示。`WorkListScreen`/`LifeListScreen` 已用 `FloatingAboveNavLocation`，书架页漏配。
- **修复**：补 `floatingActionButtonLocation: FloatingAboveNavLocation()`，两入口 FAB 位置统一、均可见。

### 5. 读书笔记编辑 + 时间显示（feat，`9b589b5`）
- **需求**（用户拍板：点笔记直接进编辑页，不设只读详情页）：
  1. 笔记可再次编辑——书籍详情页笔记项点击进入编辑页（此前仅长按删除）。
  2. 列表显示时间——格式增强：今天 → `今天 HH:mm`；今年 → `M月d日`；跨年 → `yyyy年M月d日`（原仅 `M/d`）。
  3. 笔记页可见首次新建时间——编辑页顶部时间信息块：`🕐 首次创建` + `✏️ 最后修改`（`yyyy年M月d日 HH:mm`）；新建笔记时不显示，标题区分「新建笔记/编辑笔记」。
- **技术要点**：编辑页复用既有 `NoteEditScreen` 编辑模式（`NoteEditArgs.note` 非空走 `updateNote`，`createdAt` 不变、`updatedAt` 自动刷新）；5 个 Tab 列表用 `GlobalKey<_NotesListState>` 统一刷新（keepAlive 列表需主动触发 `_loadNotes`）；`strings.dart` 新增「编辑笔记/首次创建/最后修改」三语言文案。

---

## 三、遗留事项

- `app-release.apk` 仍为 7-30 构建，未更新。
- 5 个提交均未推送（待用户决定）。
