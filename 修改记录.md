# BuildSelf APP 修改记录

---

## 1. APP 图标设置

**目标**：将 `Hero Illustration.png` 设置为 APP 图标

| 文件 | 修改内容 |
|------|----------|
| `pubspec.yaml#L32` | 在 `dev_dependencies` 中添加 `flutter_launcher_icons: ^0.13.1` |
| `pubspec.yaml#L49-L53` | 新增 `flutter_launcher_icons` 配置段，指定 `image_path: "Hero Illustration.png"`，启用 Android 和 iOS |

**待执行命令**（因 PowerShell 策略限制未运行）：

```bash
flutter pub get
dart run flutter_launcher_icons
```

---

## 2. Android APP 名称修改

**目标**：将名称从 `buildself` 改为 `BuildSelf`

| 文件 | 修改内容 |
|------|----------|
| `android/app/src/main/AndroidManifest.xml#L3` | `android:label="buildself"` → `android:label="BuildSelf"` |

---

## 3. 底部 Tab 导航调整

**目标**：Tab 改为「首页、工作、生活、目标、阅读」，移除「我的」，设置入口移至首页右上角

| 文件 | 修改内容 |
|------|----------|
| `lib/core/constants/strings.dart#L22` | 新增 `tabLife = '生活'` 常量 |
| `lib/shared/layouts/main_scaffold.dart` | 1. 引入 `LifeListScreen`，移除 `SettingsScreen`<br>2. `_pages` 列表调整为 5 页：首页、工作、生活、目标、阅读<br>3. 底部导航 items 对应调整为：home、work、favorite、flag、menu_book<br>4. 添加 `type: BottomNavigationBarType.fixed` |

**设置页入口**：`lib/features/home/screens/home_screen.dart#L161-L164` 已有右上角设置图标，点击跳转 `AppRoutes.settings`，无需额外修改。

---

## 4. 关于页文案修改

**目标**：调整关于页显示的应用名称、标语与底部文案

| 文件 | 修改内容 |
|------|----------|
| `lib/core/constants/app_constants.dart#L6` | `appName` 由 `'Buildself'` 改为 `'BuildSelf'` |
| `lib/core/constants/strings.dart#L7` | `appSlogan` 改为 `'想建设新中国一样建设自己'` |
| `lib/features/settings/screens/about_screen.dart#L46-L50` | 硬编码文案改为 `'建设自己，是一场最值得的投资'` |

---

## 5. 模块图标和颜色调整

**目标**：调整底部 Tab 导航和各模块页面的图标与颜色

### 5.1 颜色定义修改

| 文件 | 修改内容 |
|------|----------|
| `lib/core/constants/colors.dart#L21-L25` | 模块颜色重新定义：<br>• work: `0xFF2196F3`（蓝色）<br>• life: `0xFFFFB300`（琥珀色）<br>• goal: `0xFF4CAF50`（绿色）<br>• reading: `0xFF607D8B`（青山色）<br>• murmur: `0xFF9C27B0`（紫色） |

### 5.2 图标修改

| 模块 | 修改前 | 修改后 |
|------|--------|--------|
| 工作 | `Icons.work_outline` | `Icons.event_outlined`（日历） |
| 生活 | `Icons.favorite_outline` | `Icons.local_cafe_outlined`（咖啡杯） |
| 目标 | `Icons.flag_outlined` | `Icons.gps_fixed_outlined`（靶心） |
| 阅读 | `Icons.menu_book_outlined` | 保持不变（书本） |
| 碎碎念 | `Icons.chat_bubble_outline` | `Icons.auto_awesome`（闪光星星） |

### 5.3 涉及文件

| 文件 | 修改内容 |
|------|----------|
| `lib/shared/layouts/main_scaffold.dart#L46-L65` | 底部导航 Tab 图标替换 |
| `lib/features/home/screens/home_screen.dart#L172-L174` | 首页快捷入口图标替换 |
| `lib/features/work/screens/work_list_screen.dart#L100` | 空状态图标替换 |
| `lib/features/life/screens/life_list_screen.dart#L104` | 空状态图标替换 |
| `lib/features/goal/screens/goal_board_screen.dart#L113` | 空状态图标替换 |
| `lib/features/murmur/screens/murmur_screen.dart#L209` | 空状态图标替换 |

---

## 6. 底部 Tab 选中颜色修复 & 视觉美化

**目标**：修复 Tab 选中时颜色不显示，并为底部导航栏添加圆角和阴影

| 文件 | 修改内容 |
|------|----------|
| `lib/shared/layouts/main_scaffold.dart#L44-L68` | 为每个 Tab 的 `activeIcon` 显式设置对应的模块色，优先级高于主题统一色 |
| `lib/shared/layouts/main_scaffold.dart#L29-L40` | 新增 `_tabColors` 列表，`selectedItemColor` 动态取当前 Tab 的模块色 |
| `lib/shared/layouts/main_scaffold.dart#L46-L97` | 底部导航栏外层包裹 `Container`，添加：<br>• 顶部圆角 `BorderRadius.vertical(top: Radius.circular(20))`<br>• 柔和阴影 `blurRadius: 16`，透明度 6%<br>• `elevation: 0` 避免双重阴影 |

---

## 7. 首页问候语固定显示

**目标**：将问候语中的用户昵称固定为「探索者」

| 文件 | 修改内容 |
|------|----------|
| `lib/features/home/screens/home_screen.dart#L146` | `'$greeting，$nickname 👋'` → `'$greeting，探索者 👋'`，不再读取用户昵称 |

---

## 8. 工作模块自定义分类

**目标**：在原有的「经验/心得/反思」基础上，支持用户新增自定义分类

| 文件 | 修改内容 |
|------|----------|
| `lib/data/models/work_note_model.dart` | `recordType` 从 `WorkRecordType` 枚举改为 `String`；新增 `_normalizeRecordType` 兼容旧数据（枚举名→中文标签） |
| `lib/data/repositories/work_repository.dart` | `create`/`getAll` 参数改为 `String`；新增 `getCategories`/`addCategory`/`removeCategory`，SharedPreferences key: `work_categories_$userId` |
| `lib/features/work/screens/work_edit_screen.dart` | 分类选择从枚举遍历改为动态列表；新增「+ 新增」按钮和弹窗 |
| `lib/features/work/screens/work_list_screen.dart` | 筛选栏动态加载分类；从编辑页返回后刷新分类；`typeColors` 移除改为统一蓝色；`recordType.label` → `recordType` |
| `lib/features/work/screens/work_detail_screen.dart#L90` | `note.recordType.label` → `note.recordType` |

### 内置分类：经验、心得、反思

---

## 9. 生活模块自定义分类

**目标**：在原有的「美好/感悟/反思」基础上，支持用户新增自定义分类

| 文件 | 修改内容 |
|------|----------|
| `lib/data/models/life_record_model.dart` | `recordType` 从 `LifeRecordType` 枚举改为 `String`；新增 `_normalizeRecordType` 兼容旧数据（枚举名→中文标签） |
| `lib/data/repositories/life_repository.dart` | `create`/`getAll` 参数改为 `String`；新增 `getCategories`/`addCategory`/`removeCategory`，SharedPreferences key: `life_categories_$userId` |
| `lib/features/life/screens/life_edit_screen.dart` | 分类选择从枚举遍历改为动态列表；新增「+ 新增」按钮和弹窗 |
| `lib/features/life/screens/life_list_screen.dart` | 筛选栏动态加载分类；卡片和详情中 `recordType.label` → `recordType`；移除多余 `enums.dart` import |

### 内置分类：美好、感悟、反思

---

## 10. 主题设置 404 修复 & 完整主题管理

**目标**：修复设置 → 主题设置跳转 404，并实现完整的主题模式切换与持久化

| 文件 | 修改内容 |
|------|----------|
| `lib/features/auth/providers/app_provider.dart` | 新增 `ThemeMode _themeMode` 字段、`themeMode` getter 和 `setThemeMode()` 方法；使用 `SharedPreferences` 持久化（key: `theme_mode`） |
| `lib/features/settings/screens/theme_setting_screen.dart` | **新建**主题设置页，提供「跟随系统 / 浅色模式 / 深色模式」三个选项，选中项带勾选标记 |
| `lib/core/router/app_router.dart#L90-L91` | 注册 `AppRoutes.theme` → `ThemeSettingScreen` 路由（之前未注册导致 404） |
| `lib/main.dart#L59-L79` | `themeMode` 从硬编码 `ThemeMode.system` 改为 `provider.themeMode`；合并内部重复的 `Consumer` |
| `lib/features/settings/screens/settings_screen.dart#L89` | 主题项副标题从硬编码"跟随系统"改为通过 `_themeModeLabel()` 动态显示当前模式 |

---

## 修改前后对比

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| APP 图标 | Flutter 默认图标 | `Hero Illustration.png`（待运行生成命令） |
| Android 名称 | `buildself` | `BuildSelf` |
| 底部 Tab | 首页、工作、目标、阅读、我的 | 首页、工作、生活、目标、阅读 |
| 设置页入口 | 底部第 5 个 Tab | 首页右上角⚙️图标进入 |
| 应用名称 | Buildself | BuildSelf |
| 标语 | 建设自己，是一场最值得的投资 | 想建设新中国一样建设自己 |
| 关于页底部文案 | 所有数据存储在本地设备<br>你的记录只属于你自己 | 建设自己，是一场最值得的投资 |
| 工作模块 | 商务蓝 + 公文包图标 | 蓝色 + 日历图标 |
| 生活模块 | 生活橙 + 爱心图标 | 琥珀色 + 咖啡杯图标 |
| 目标模块 | 活力绿 + 旗帜图标 | 绿色 + 靶心图标 |
| 阅读模块 | 书卷棕 + 书本图标 | 青山色 + 书本图标 |
| 碎碎念模块 | 柔粉 + 对话气泡图标 | 紫色 + 闪光星星图标 |
| 底部导航栏外观 | 白色平板，无阴影圆角 | 顶部圆角 20px + 柔和上浮阴影 |
| Tab 选中颜色 | 统一绿色覆盖 | 各模块专属色（蓝/琥珀/绿/青山） |
| 首页问候语 | `下午好，用户2728 👋`（读取昵称） | `下午好，探索者 👋`（固定） |
| 工作分类 | 经验 / 心得 / 反思（固定） | 经验 / 心得 / 反思 + 用户自定义 |
| 生活分类 | 美好 / 感悟 / 反思（固定） | 美好 / 感悟 / 反思 + 用户自定义 |
| 主题设置 | 点击跳转 404 | 正常显示，支持跟随系统/浅色/深色并持久化 |
