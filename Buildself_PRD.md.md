# Buildself — 个人成长记录 APP 产品需求文档（PRD）

| 属性 | 内容 |
|------|------|
| 产品名称 | Buildself |
| 文档版本 | v1.0 |
| 编写日期 | 2026-07-30 |
| 目标平台 | Android / iOS |
| 技术栈 | Flutter + 本地存储 |
| 文档状态 | 初稿 |

---

## 目录

1. [产品概述](#1-产品概述)
2. [目标用户与使用场景](#2-目标用户与使用场景)
3. [产品架构总览](#3-产品架构总览)
4. [功能模块详细设计](#4-功能模块详细设计)
   - 4.1 [工作模块](#41-工作模块)
   - 4.2 [生活模块](#42-生活模块)
   - 4.3 [目标管理模块](#43-目标管理模块)
   - 4.4 [阅读模块](#44-阅读模块)
   - 4.5 [碎碎念模块](#45-碎碎念模块)
5. [登录与用户体系](#5-登录与用户体系)
6. [数据存储与安全方案](#6-数据存储与安全方案)
7. [UI/UX 设计规范](#7-uiux-设计规范)
8. [技术架构设计](#8-技术架构设计)
9. [非功能性需求](#9-非功能性需求)
10. [开发计划与里程碑](#10-开发计划与里程碑)
11. [打包发布与上架策略](#11-打包发布与上架策略)
12. [后续迭代规划](#12-后续迭代规划)
13. [附录](#13-附录)

---

## 1. 产品概述

### 1.1 产品愿景

> **Buildself — 建设自己，是一场最值得的投资。**

Buildself 是一款专注于「个人成长记录」的移动端 APP。它不追求社交、不追求流量，而是为用户提供一个安静的、私密的、属于自己的空间，帮助他们持续记录工作中的经验与反思、生活中的美好与感悟、目标达成的每一步、阅读带来的思想改变，以及日常的碎碎念。

通过持续记录与回顾，用户能够清晰地看到自己的成长轨迹，建立「自我对话」的习惯，在日积月累中完成从「被动经历生活」到「主动建设自己」的转变。

### 1.2 核心价值主张

| 价值维度 | 说明 |
|----------|------|
| **私密性** | 所有数据存储在本地设备，不经服务器中转，用户完全拥有自己的数据 |
| **结构化** | 五大模块覆盖个人成长的核心场景，记录不再是零散的流水账 |
| **仪式感** | 目标与奖励联动、阅读改变记录，赋予成长过程以仪式感 |
| **可追溯** | 时间线、标签、搜索功能，让用户随时回溯自己的成长轨迹 |
| **跨平台** | Android 与 iOS 双端可用，覆盖主流移动设备 |

### 1.3 产品定位

- **产品类型**：个人效率 / 笔记工具类 APP
- **核心关键词**：个人成长、记录、反思、目标管理、阅读笔记
- **差异化**：不同于通用笔记应用（如备忘录、Notion），Buildself 以「成长」为核心叙事，五大模块围绕个人成长场景深度定制，而非提供空白画布让用户自行组织
- **目标规模**：个人使用 + 小范围分享，初期不涉及大规模商业化运营

---

## 2. 目标用户与使用场景

### 2.1 目标用户画像

| 维度 | 描述 |
|------|------|
| **年龄段** | 22-40 岁 |
| **职业** | 职场新人、中级从业者、自由职业者、持续学习者 |
| **特征** | 有自我提升意识、习惯反思、重视个人成长、注重隐私 |
| **痛点** | 工作经验零散难以沉淀；生活感悟转瞬即逝；目标设定后容易遗忘；读书后缺乏系统记录；情绪和想法无处安放 |
| **使用频率** | 每日 1-3 次，单次使用 2-10 分钟 |

### 2.2 典型使用场景

**场景一：工作反思**
> 下班后，小A打开 Buildself 工作模块，记录今天项目中遇到的问题和解决思路。他给这条记录打上「沟通」「项目管理」标签，方便日后检索。三个月后做季度复盘时，他通过标签筛选快速回顾了这段时间的所有工作反思。

**场景二：目标激励**
> 小B设定了一个中期目标「完成一次半程马拉松」，奖励是「买一双心仪已久的跑鞋」。每次训练后她在目标下更新进度，看到进度条从 30% 推进到 60%，想到完成时就能得到那双跑鞋，动力十足。

**场景三：阅读沉淀**
> 小C读完《原子习惯》后，在阅读模块记录了核心观点摘抄、自己的思考，以及「决定从明天开始每天早起 15 分钟」的行动改变。半年后回看，他发现这个小小的改变确实重塑了自己的早晨习惯。

**场景四：碎碎念**
> 深夜失眠，小D打开碎碎念模块，随手写下对未来的焦虑和对当下的感恩。写完之后心绪平静了许多。这些文字不会被任何人看到，是完全属于自己的情绪出口。

**场景五：生活美好**
> 周末在公园散步，小E看到夕阳很美，打开生活模块快速记录下这一刻的感受和一张随手拍的照片。这些微小但美好的瞬间，构成了她的「生活高光集」。

---

## 3. 产品架构总览

### 3.1 功能架构图

```
Buildself
│
├── 登录/注册
│   ├── 微信登录
│   └── 手机号登录
│
├── 首页（成长概览）
│   ├── 今日记录快捷入口
│   ├── 近期目标进度
│   ├── 本周记录统计
│   └── 精选回顾（往日今日）
│
├── 工作模块
│   ├── 记录列表（时间线/标签筛选）
│   ├── 新建/编辑记录
│   ├── 标签管理
│   └── 搜索
│
├── 生活模块
│   ├── 记录列表（时间线/相册视图）
│   ├── 新建/编辑记录（支持图片）
│   └── 搜索
│
├── 目标管理模块
│   ├── 目标看板（短期/中期/长期）
│   ├── 新建/编辑目标
│   ├── 进度更新
│   ├── 奖励设置与展示
│   └── 目标达成记录
│
├── 阅读模块
│   ├── 书架（已读/在读）
│   ├── 新建读书笔记
│   ├── 笔记详情（摘抄/心得/思考/改变）
│   └── 搜索
│
├── 碎碎念模块
│   ├── 时间线流
│   ├── 快速记录
│   └── 搜索
│
├── 我的
│   ├── 个人信息
│   ├── 数据备份与恢复
│   ├── 数据导出
│   ├── 主题设置
│   ├── 隐私与安全（应用锁）
│   └── 关于
│
└── 全局功能
    ├── 全局搜索
    ├── 通知提醒
    └── 应用锁
```

### 3.2 导航结构

采用 **底部 Tab 导航 + 模块内页面栈** 的结构：

| Tab | 模块 | 图标语义 |
|-----|------|----------|
| Tab 1 | 首页 | 房屋 / 仪表盘 |
| Tab 2 | 工作 | 公文包 |
| Tab 3 | 目标 | 靶心 / 旗帜 |
| Tab 4 | 阅读 | 书本 |
| Tab 5 | 我的 | 用户轮廓 |

> 生活模块和碎碎念模块通过首页快捷入口进入，不单独占用底部 Tab 位（5个Tab是用户体验上限）。也可在设置中允许用户自定义底部 Tab 的模块分配。

---

## 4. 功能模块详细设计

### 4.1 工作模块

#### 4.1.1 模块定位

记录工作中的经验、心得与反思，帮助用户系统化沉淀职业成长。

#### 4.1.2 记录字段定义

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String (UUID) | 是 | 记录唯一标识 |
| `title` | String | 否 | 标题，可为空（碎碎念式记录） |
| `content` | Text (富文本) | 是 | 正文内容 |
| `recordType` | Enum | 是 | `experience`(经验) / `insight`(心得) / `reflection`(反思) |
| `tags` | List\<String\> | 否 | 自定义标签，如「沟通」「时间管理」 |
| `mood` | Enum | 否 | 记录时的心情：😊 😐 😤 😢 🤔 |
| `createdAt` | DateTime | 是 | 创建时间 |
| `updatedAt` | DateTime | 是 | 最后更新时间 |
| `attachments` | List\<ImageRef\> | 否 | 附带图片（本地路径引用） |

#### 4.1.3 功能点

| 编号 | 功能 | 描述 |
|------|------|------|
| W-01 | 新建记录 | 选择类型（经验/心得/反思）→ 填写内容 → 添加标签 → 保存 |
| W-02 | 编辑记录 | 支持随时编辑已保存的记录 |
| W-03 | 删除记录 | 软删除（移入回收站，30天后永久清除） |
| W-04 | 记录列表 | 默认按时间倒序展示，支持切换「时间线视图」和「分组视图（按类型）」 |
| W-05 | 标签筛选 | 点击标签筛选出包含该标签的所有记录 |
| W-06 | 标签管理 | 在标签列表页可重命名、合并、删除标签 |
| W-07 | 搜索 | 支持按关键词、标签、类型、日期范围组合搜索 |
| W-08 | 富文本编辑 | 支持加粗、斜体、列表、引用、分割线等基础格式 |
| W-09 | 快速记录 | 从首页快捷入口直接进入新建页面，默认类型为「心得」 |

#### 4.1.4 交互流程

```
工作模块首页
  ├── [+] 新建记录 → 选择类型 → 编辑页 → 保存
  ├── 点击记录 → 详情页 → [编辑] / [删除]
  ├── [筛选] → 按类型/标签筛选
  └── [搜索] → 输入关键词 → 结果列表
```

---

### 4.2 生活模块

#### 4.2.1 模块定位

记录生活中的美好瞬间、感悟与反思，构建个人的「生活高光集」。

#### 4.2.2 记录字段定义

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String (UUID) | 是 | 记录唯一标识 |
| `title` | String | 否 | 标题 |
| `content` | Text (富文本) | 是 | 正文内容 |
| `recordType` | Enum | 是 | `beauty`(美好) / `insight`(感悟) / `reflection`(反思) |
| `mood` | Enum | 否 | 心情表情 |
| `weather` | Enum | 否 | 天气：☀️ ☁️ 🌧️ ❄️ 🌫️ |
| `location` | String | 否 | 地点（手动输入或选择，不调用定位SDK） |
| `images` | List\<ImageRef\> | 否 | 照片（最多9张） |
| `tags` | List\<String\> | 否 | 自定义标签 |
| `createdAt` | DateTime | 是 | 创建时间 |
| `updatedAt` | DateTime | 是 | 最后更新时间 |

#### 4.2.3 功能点

| 编号 | 功能 | 描述 |
|------|------|------|
| L-01 | 新建记录 | 选择类型 → 编写内容 → 添加照片 → 设置天气/心情/地点 → 保存 |
| L-02 | 图片管理 | 支持从相册选择（最多9张）、拍照、预览、删除、拖拽排序 |
| L-03 | 相册视图 | 列表页支持切换「时间线视图」和「相册视图（瀑布流展示所有照片）」 |
| L-04 | 编辑/删除 | 同工作模块 |
| L-05 | 搜索筛选 | 支持按关键词、标签、类型、心情、日期搜索 |
| L-06 | 往日今日 | 首页展示「历史上的今天」曾记录的生活片段 |

---

### 4.3 目标管理模块

#### 4.3.1 模块定位

帮助用户设定短期/中期/长期目标，并通过自定义奖励机制提供持续动力。

#### 4.3.2 目标字段定义

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String (UUID) | 是 | 目标唯一标识 |
| `title` | String | 是 | 目标标题 |
| `description` | Text | 否 | 目标详细描述 |
| `goalType` | Enum | 是 | `short_term`(短期) / `mid_term`(中期) / `long_term`(长期) |
| `category` | Enum | 否 | 目标类别：`health`(健康) / `career`(事业) / `learning`(学习) / `life`(生活) / `finance`(财务) / `other`(其他) |
| `startDate` | DateTime | 是 | 开始日期 |
| `targetDate` | DateTime | 否 | 目标完成日期 |
| `progressType` | Enum | 是 | `manual`(手动百分比) / `milestone`(里程碑式) / `checklist`(清单式) |
| `progress` | int (0-100) | 否 | 当前进度百分比（manual模式） |
| `milestones` | List\<Milestone\> | 否 | 里程碑列表（milestone模式） |
| `checklist` | List\<ChecklistItem\> | 否 | 清单项列表（checklist模式） |
| `reward` | Reward | 是 | 完成目标后的奖励 |
| `status` | Enum | 是 | `active`(进行中) / `completed`(已完成) / `abandoned`(已放弃) |
| `createdAt` | DateTime | 是 | 创建时间 |
| `updatedAt` | DateTime | 是 | 更新时间 |
| `completedAt` | DateTime | 否 | 完成时间 |

**Milestone 结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | String | 里程碑ID |
| `title` | String | 里程碑标题 |
| `isCompleted` | bool | 是否完成 |
| `completedAt` | DateTime | 完成时间 |

**ChecklistItem 结构：**

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | String | 清单项ID |
| `title` | String | 清单项内容 |
| `isCompleted` | bool | 是否完成 |

**Reward 结构：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `type` | Enum | 是 | `food`(美食) / `travel`(旅行) / `shopping`(购物) / `experience`(体验) / `other`(其他) |
| `description` | String | 是 | 奖励描述，如「一顿海底捞」「一双Nike跑鞋」「去日本旅行」 |
| `image` | ImageRef | 否 | 奖励图片（如想买的衣服照片、想去的旅行地图片） |
| `estimatedCost` | double | 否 | 预估花费（可选，仅自己可见） |

#### 4.3.3 功能点

| 编号 | 功能 | 描述 |
|------|------|------|
| G-01 | 新建目标 | 填写标题 → 选择类型（短/中/长期）→ 设置日期 → 选择进度模式 → 设置奖励 → 保存 |
| G-02 | 目标看板 | 分栏展示短期/中期/长期目标，按状态（进行中/已完成）分类 |
| G-03 | 进度更新 | manual模式：拖动滑块或输入百分比；milestone模式：勾选里程碑；checklist模式：勾选清单项，自动计算进度 |
| G-04 | 奖励展示 | 目标详情页醒目展示奖励信息，进度达100%时触发庆祝动画并解锁奖励领取提示 |
| G-05 | 目标达成庆祝 | 完成目标时弹出庆祝弹窗（彩带/烟花动画），展示奖励内容，可选择「领取奖励」或「稍后」 |
| G-06 | 目标日志 | 每次进度更新自动记录时间戳和进度变化，形成目标推进时间线 |
| G-07 | 目标编辑/删除 | 支持随时编辑目标信息；删除为软删除 |
| G-08 | 目标归档 | 已完成的目标自动归入「成就墙」，可随时查看历史达成记录 |
| G-09 | 截止提醒 | 接近截止日期时发送本地通知提醒（可设置提前天数） |
| G-10 | 目标关联 | 可将目标与工作/阅读模块的记录关联（可选，高级功能） |

#### 4.3.4 交互流程

```
目标看板
  ├── [短/中/长] Tab切换
  ├── [+] 新建目标 → 编辑页 → 保存
  ├── 点击目标卡片 → 目标详情
  │     ├── 目标信息
  │     ├── 奖励展示卡（带图片）
  │     ├── 进度条 + [更新进度]
  │     ├── 目标日志时间线
  │     └── [编辑] / [删除] / [标记完成]
  └── [成就墙] → 查看已完成目标列表
```

---

### 4.4 阅读模块

#### 4.4.1 模块定位

记录读书笔记、心得、摘抄、思考以及从书中获得的改变，让阅读真正转化为成长。

#### 4.4.2 数据模型

**Book（书籍）结构：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String (UUID) | 是 | 书籍唯一标识 |
| `title` | String | 是 | 书名 |
| `author` | String | 否 | 作者 |
| `coverImage` | ImageRef | 否 | 封面图片（本地存储或手动输入ISBN后展示占位图） |
| `status` | Enum | 是 | `reading`(在读) / `finished`(已读) / `paused`(暂停) / `planned`(想读) |
| `startDate` | DateTime | 否 | 开始阅读日期 |
| `finishDate` | DateTime | 否 | 读完日期 |
| `rating` | int (1-5) | 否 | 评分（五星制） |
| `tags` | List\<String\> | 否 | 分类标签 |
| `createdAt` | DateTime | 是 | 创建时间 |

**ReadingNote（读书笔记）结构：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String (UUID) | 是 | 笔记唯一标识 |
| `bookId` | String | 是 | 关联书籍ID |
| `chapter` | String | 否 | 章节/页码 |
| `noteType` | Enum | 是 | `excerpt`(摘抄) / `insight`(心得) / `thought`(思考) / `change`(改变) |
| `content` | Text | 是 | 笔记内容 |
| `createdAt` | DateTime | 是 | 创建时间 |
| `updatedAt` | DateTime | 是 | 更新时间 |

> 四种笔记类型对应阅读的四个层次：
> - **摘抄**：书中原文的精华摘录
> - **心得**：对书中内容的理解和感悟
> - **思考**：由书中内容引发的延伸思考
> - **改变**：因这本书而决定做出的行动改变

#### 4.4.3 功能点

| 编号 | 功能 | 描述 |
|------|------|------|
| R-01 | 添加书籍 | 输入书名/作者 → 设置状态（想读/在读）→ 保存 |
| R-02 | 书架展示 | 网格视图展示所有书籍，封面+状态标记，支持按状态/评分/时间排序 |
| R-03 | 书籍详情 | 展示书籍信息 + 该书所有笔记（按类型分组Tab） |
| R-04 | 新建笔记 | 在书籍详情页内新建，选择笔记类型 → 编写内容 → 保存 |
| R-05 | 笔记类型筛选 | 在书籍详情页按「摘抄/心得/思考/改变」筛选笔记 |
| R-06 | 「改变」专项卡 | 每本书的「改变」类笔记汇总为行动清单，首页可展示「近期改变」 |
| R-07 | 搜索 | 全局搜索笔记内容，或按书名/作者/标签筛选 |
| R-08 | 阅读统计 | 统计已读书籍数量、笔记总数、本月阅读情况 |
| R-09 | 编辑/删除 | 同其他模块 |

#### 4.4.4 交互流程

```
阅读模块首页（书架）
  ├── [+] 添加书籍
  ├── 点击书籍 → 书籍详情
  │     ├── 书籍信息卡（封面/标题/作者/评分/状态）
  │     ├── [全部/摘抄/心得/思考/改变] Tab
  │     ├── 笔记列表
  │     └── [+] 新建笔记
  └── [搜索] → 输入关键词
```

---

### 4.5 碎碎念模块

#### 4.5.1 模块定位

一个轻量、低门槛的记录空间，随手记录随笔、随想、随感，是情绪出口和灵感捕捉器。

#### 4.5.2 记录字段定义

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `id` | String (UUID) | 是 | 记录唯一标识 |
| `content` | Text | 是 | 正文内容（纯文本，支持换行） |
| `mood` | Enum | 否 | 心情表情 |
| `tags` | List\<String\> | 否 | 标签 |
| `images` | List\<ImageRef\> | 否 | 图片（最多3张，轻量化） |
| `createdAt` | DateTime | 是 | 创建时间 |
| `updatedAt` | DateTime | 是 | 更新时间 |

#### 4.5.3 功能点

| 编号 | 功能 | 描述 |
|------|------|------|
| M-01 | 快速记录 | 进入页面即显示输入框，写完点「记下」即保存，极简流程 |
| M-02 | 时间线流 | 按日期分组倒序展示，每条记录显示时间戳和心情 |
| M-03 | 心情标记 | 记录时可选心情表情，时间线上以心情色块点缀 |
| M-04 | 编辑/删除 | 长按或点击进入编辑，支持删除 |
| M-05 | 搜索 | 按关键词搜索碎碎念内容 |
| M-06 | 日期跳转 | 支持按日期快速跳转查看历史记录 |
| M-07 | 字数统计 | 展示总记录数和总字数 |

#### 4.5.4 设计理念

碎碎念模块的设计核心是 **「低门槛、快记录、零压力」**：

- 不要求标题，打开即写
- 不要求选择类型，减少决策成本
- 支持语音输入（调用系统输入法语音功能）
- 视觉上营造「日记本」的质感，温暖、私密

---

## 5. 登录与用户体系

### 5.1 登录方案设计

> **核心矛盾**：用户要求支持微信/手机号登录，同时选择纯本地存储方案（不建后端）。
>
> **解决思路**：将「登录」从「云端身份认证」降级为「本地身份标识」。用户信息仅存储在本地，不与云端交互。登录的作用是建立本地用户档案、启用应用锁保护、支持数据备份标识。

#### 5.1.1 微信登录（本地化适配）

| 方面 | 方案 |
|------|------|
| SDK | 集成微信开放平台 SDK（fluwx 插件） |
| 流程 | 用户点击微信登录 → 唤起微信 App 授权 → 获取 openid 和昵称头像 → **仅存本地**，不上传服务器 |
| 作用 | 获取用户标识用于本地数据绑定；显示用户昵称头像 |
| 注意 | 因无后端，微信登录的「跨设备同步」不可用；同一设备重新安装后需重新登录并恢复备份 |
| 降级 | 若微信 SDK 授权失败（未安装微信/网络问题），引导使用手机号登录 |

#### 5.1.2 手机号登录（本地化适配）

| 方面 | 方案 |
|------|------|
| 流程 | 用户输入手机号 → 本地验证码（模拟，见下文）→ 验证通过 → 创建/匹配本地用户 |
| 验证码 | **方案A（推荐）**：首次使用时不发真实短信，输入任意6位数字即可通过（纯本地模式）；**方案B（后续升级）**：接入短信服务（需后端），发送真实验证码 |
| 存储 | 手机号 + 本地生成的 userId，加密存储在本地 |
| 作用 | 作为本地用户唯一标识，支持数据备份时的用户识别 |

#### 5.1.3 本地用户模型

| 字段 | 类型 | 说明 |
|------|------|------|
| `userId` | String | 本地生成的 UUID |
| `loginType` | Enum | `wechat` / `phone` |
| `phone` | String? | 手机号（加密存储） |
| `wechatOpenId` | String? | 微信 openid |
| `nickname` | String | 昵称 |
| `avatar` | ImageRef? | 头像（本地路径） |
| `createdAt` | DateTime | 注册时间 |
| `lastLoginAt` | DateTime | 最后登录时间 |

### 5.2 登录流程图

```
首次打开 APP
  └── 欢迎页 → 登录页
        ├── [微信登录] → 唤起微信 → 授权 → 获取信息 → 创建本地用户 → 进入首页
        └── [手机号登录] → 输入手机号 → 输入验证码 → 验证通过 → 创建本地用户 → 进入首页

后续打开 APP
  └── 检查本地用户信息
        ├── 已登录 → [应用锁验证] → 进入首页
        └── 未登录 → 登录页
```

### 5.3 关于登录的诚实说明

由于选择纯本地存储方案，需向用户明确告知以下限制：

| 限制项 | 说明 |
|--------|------|
| 无法跨设备同步 | 登录信息仅存本地，换设备需重新登录并手动恢复备份 |
| 微信登录的意义 | 主要用于获取昵称头像和本地身份标识，不提供云端账号体系 |
| 数据备份 | 提供本地导出功能（加密文件），用户可手动传输到新设备恢复 |
| 后续升级路径 | 若未来需要真正的多设备同步，需补建后端服务，届时已有本地数据可平滑迁移 |

---

## 6. 数据存储与安全方案

### 6.1 本地存储技术选型

| 方案 | 说明 | 选型 |
|------|------|------|
| **sqflite** | SQLite 的 Flutter 插件，支持复杂查询、事务、关系型数据 | ✅ **主选** |
| Hive | 轻量 NoSQL 键值存储，速度快但不支持复杂查询 | 作为缓存层补充 |
| SharedPreferences | 存储简单配置项（主题、应用锁开关等） | ✅ 配置存储 |
| 文件系统 | 图片等媒体文件存储在 App 沙箱目录 | ✅ 媒体存储 |

> **选型理由**：Buildself 的数据模型具有明显的关系型特征（目标-里程碑-日志、书籍-笔记等关联），需要多表联查、标签筛选等复杂查询能力，SQLite 是最佳选择。

### 6.2 数据库设计

#### 6.2.1 数据库表结构概览

```
┌─────────────┐     ┌──────────────┐     ┌──────────────────┐
│   users     │     │  work_notes  │     │  life_records    │
├─────────────┤     ├──────────────┤     ├──────────────────┤
│ id (PK)     │     │ id (PK)      │     │ id (PK)          │
│ login_type  │     │ user_id (FK) │     │ user_id (FK)     │
│ phone       │     │ title        │     │ title            │
│ nickname    │     │ content      │     │ content          │
│ avatar_path │     │ record_type  │     │ record_type      │
│ created_at  │     │ mood         │     │ mood             │
│ last_login  │     │ tags (JSON)  │     │ weather          │
└─────────────┘     │ created_at   │     │ location         │
                    │ updated_at   │     │ tags (JSON)      │
                    │ deleted_at   │     │ created_at       │
                    └──────────────┘     │ updated_at       │
                                         │ deleted_at       │
┌──────────────────┐ ┌──────────────────┐└──────────────────┘
│     goals        │ │     books        │
├──────────────────┤ ├──────────────────┤ ┌──────────────────┐
│ id (PK)          │ │ id (PK)          │ │  reading_notes   │
│ user_id (FK)     │ │ user_id (FK)     │ ├──────────────────┤
│ title            │ │ title            │ │ id (PK)          │
│ description      │ │ author           │ │ book_id (FK)     │
│ goal_type        │ │ cover_path       │ │ note_type        │
│ category         │ │ status           │ │ chapter          │
│ start_date       │ │ start_date       │ │ content          │
│ target_date      │ │ finish_date      │ │ created_at       │
│ progress_type    │ │ rating           │ │ updated_at       │
│ progress         │ │ tags (JSON)      │ └──────────────────┘
│ milestones(JSON) │ │ created_at       │
│ checklist (JSON) │ └──────────────────┘ ┌──────────────────┐
│ reward (JSON)    │                       │   murmurs        │
│ status           │                       ├──────────────────┤
│ created_at       │                       │ id (PK)          │
│ updated_at       │                       │ user_id (FK)     │
│ completed_at     │                       │ content          │
│ deleted_at       │                       │ mood             │
└──────────────────┘                       │ tags (JSON)      │
                                           │ images (JSON)    │
┌──────────────────┐                       │ created_at       │
│  goal_logs       │                       │ updated_at       │
├──────────────────┤                       │ deleted_at       │
│ id (PK)          │                       └──────────────────┘
│ goal_id (FK)     │
│ progress_before  │ ┌──────────────────┐  ┌──────────────────┐
│ progress_after   │ │   attachments    │  │   trash_items    │
│ note             │ ├──────────────────┤  ├──────────────────┤
│ created_at       │ │ id (PK)          │  │ id (PK)          │
└──────────────────┘ │ module_type      │  │ module_type      │
                    │ record_id        │  │ record_data(JSON)│
                    │ file_path        │  │ deleted_at       │
                    │ created_at       │  │ auto_purge_at    │
                    └──────────────────┘  └──────────────────┘
```

#### 6.2.2 标签存储策略

标签以 JSON 数组形式存储在记录的 `tags` 字段中，同时维护一张 `tags` 索引表用于标签管理：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | String | 标签ID |
| `name` | String | 标签名 |
| `module` | Enum | 所属模块 |
| `color` | String | 标签颜色 |
| `usage_count` | int | 使用次数（冗余字段，加速排序） |

#### 6.2.3 软删除与回收站

- 所有记录删除时设置 `deleted_at` 字段，不从数据库物理删除
- 回收站统一管理所有模块的已删除记录
- 30天后自动永久清除（应用启动时检查 `auto_purge_at`）
- 用户可手动恢复或永久删除

### 6.3 数据安全

#### 6.3.1 数据加密

| 层面 | 方案 |
|------|------|
| 数据库加密 | 使用 **SQLCipher**（sqflite_sqlcipher 插件）对 SQLite 数据库进行 AES-256 加密 |
| 密钥管理 | 加密密钥基于用户登录信息 + 设备唯一标识生成，存储在系统 Keychain(iOS) / Keystore(Android) |
| 敏感字段 | 手机号等敏感信息单独加密后存储 |
| 图片文件 | 存储在 App 沙箱目录，系统自动隔离，无需额外加密（应用锁保护访问） |

#### 6.3.2 应用锁

| 功能 | 说明 |
|------|------|
| 锁定方式 | 支持 PIN 码、指纹(Face ID/Touch ID)、或两者组合 |
| 自动锁定 | 应用切到后台后自动锁定，返回需验证 |
| 模块级锁定 | 可设置仅「碎碎念」「生活」等私密模块需要二次验证 |
| 实现方案 | 使用 `local_auth` 插件实现生物识别，`flutter_secure_storage` 存储 PIN 码 |

#### 6.3.3 数据备份与恢复

| 功能 | 说明 |
|------|------|
| 本地备份 | 将数据库 + 媒体文件打包为 `.buildself` 加密归档文件 |
| 导出路径 | 保存到设备本地存储 / 通过系统分享发送到其他应用 |
| 恢复 | 选择 `.buildself` 文件 → 验证密钥 → 解密恢复数据 |
| 自动备份 | 可开启每周自动备份提醒（非自动执行，提醒用户手动备份） |
| 导出格式 | 支持导出为 Markdown 文件（按模块组织），便于在其它工具中查看 |

---

## 7. UI/UX 设计规范

### 7.1 设计理念

> **安静、温暖、有质感 — 一个让人愿意打开的私密空间。**

Buildself 的设计不是冷冰冰的工具风，而是带有「手账日记本」的温度感。每一次打开都像翻开自己的成长笔记本。

### 7.2 设计风格

| 维度 | 方案 |
|------|------|
| 整体风格 | 扁平化 + 微质感（柔和阴影、圆角卡片） |
| 设计语言 | Material Design 3 (Material You) + 自定义品牌调整 |
| 氛围 | 安静、温暖、专注、私密 |
| 参考产品 | Flomo（简洁记录）、Notion（结构化）、Day One（日记质感） |

### 7.3 配色方案

#### 7.3.1 主色调

| 色彩角色 | 色值 | 用途 |
|----------|------|------|
| 主色 | `#5B8C5A` (草木绿) | 品牌色、主按钮、Tab选中 |
| 主色浅 | `#A8D0A5` | 背景、卡片装饰 |
| 主色深 | `#3D6B3C` | 深色按钮、标题强调 |
| 辅色 | `#E8A87C` (暖橙) | 强调、进度条、奖励标记 |
| 辅色浅 | `#F5D5BE` | 奖励卡片背景 |

> **色彩语义**：草木绿象征生长与自然，呼应「Buildself 成长」主题；暖橙象征温暖与动力，用于目标与奖励相关场景。

#### 7.3.2 中性色

| 色彩角色 | 色值（浅色模式） | 色值（深色模式） |
|----------|------------------|------------------|
| 背景 | `#FAFAF7` (暖白) | `#1A1A18` |
| 卡片背景 | `#FFFFFF` | `#2A2A28` |
| 主文字 | `#2C2C2C` | `#E8E8E6` |
| 次文字 | `#888888` | `#AAAAAA` |
| 分割线 | `#E8E8E5` | `#3A3A38` |
| 占位符 | `#CCCCCC` | `#555555` |

#### 7.3.3 模块专属色

每个模块有自己的标识色，用于模块切换时的视觉区分：

| 模块 | 标识色 | 色值 |
|------|--------|------|
| 工作 | 商务蓝 | `#4A6FA5` |
| 生活 | 生活橙 | `#E8A87C` |
| 目标 | 活力绿 | `#5B8C5A` |
| 阅读 | 书卷棕 | `#8B6F47` |
| 碎碎念 | 柔粉 | `#D4A5A5` |

#### 7.3.4 深色模式

- 全面支持深色模式，跟随系统或手动切换
- 深色模式背景使用偏暖的深色（非纯黑），保护阅读舒适度
- 所有配色通过主题系统统一管理

### 7.4 字体规范

| 字号角色 | 大小 | 字重 | 用途 |
|----------|------|------|------|
| H1 | 24sp | Bold | 页面标题 |
| H2 | 20sp | SemiBold | 区块标题 |
| H3 | 17sp | SemiBold | 卡片标题 |
| Body | 15sp | Regular | 正文 |
| Caption | 13sp | Regular | 辅助说明 |
| Label | 12sp | Medium | 标签、徽章 |

| 字体族 | 平台 | 字体 |
|--------|------|------|
| 中文 | Android | Noto Sans SC |
| 中文 | iOS | PingFang SC (系统默认) |
| 英文/数字 | 双端 | Inter |

### 7.5 间距与圆角

| 元素 | 数值 |
|------|------|
| 页面边距 | 16dp |
| 卡片内边距 | 16dp |
| 卡片间距 | 12dp |
| 卡片圆角 | 12dp |
| 按钮圆角 | 8dp |
| 输入框圆角 | 8dp |
| 标签圆角 | 4dp (全高) |

### 7.6 各模块页面布局说明

#### 7.6.1 首页

```
┌─────────────────────────────────┐
│  ☰  Buildself            🔍 ⚙️  │  ← 顶栏
├─────────────────────────────────┤
│                                 │
│  下午好，小A 👋                  │  ← 问候语
│  今天是 2026年7月30日            │
│                                 │
│  ┌──────┐ ┌──────┐ ┌──────┐    │
│  │ 工作  │ │ 生活  │ │ 碎碎念│    │  ← 快捷记录入口
│  │  3条  │ │  5条  │ │  快记 │    │
│  └──────┘ └──────┘ └──────┘    │
│                                 │
│  📌 进行中的目标                  │  ← 目标进度卡
│  ┌─────────────────────────┐   │
│  │ 完成半程马拉松           │   │
│  │ ████████░░░░  60%       │   │
│  │ 🎁 奖励：一双Nike跑鞋    │   │
│  └─────────────────────────┘   │
│                                 │
│  📖 近期改变                     │  ← 阅读改变卡
│  ┌─────────────────────────┐   │
│  │ 《原子习惯》             │   │
│  │ "每天早起15分钟"          │   │
│  └─────────────────────────┘   │
│                                 │
│  📅 往日今日                     │  ← 历史记录回顾
│  ┌─────────────────────────┐   │
│  │ 2025年的今天             │   │
│  │ "今天学会了..."           │   │
│  └─────────────────────────┘   │
│                                 │
├─────────────────────────────────┤
│  🏠首页  💼工作  🎯目标  📖阅读  👤 │  ← 底部Tab
└─────────────────────────────────┘
```

#### 7.6.2 目标管理 - 目标详情页

```
┌─────────────────────────────────┐
│  ← 返回              ⋯ 更多     │
├─────────────────────────────────┤
│                                 │
│  🎯 完成半程马拉松               │  ← 目标标题
│  中期目标 · 健康 · 2026.10.15截止│  ← 元信息
│                                 │
│  进度                            │
│  ████████████░░░░░░  60%        │  ← 进度条
│  [更新进度]                      │
│                                 │
│  ┌─────────────────────────┐   │
│  │  🎁 奖励                 │   │  ← 奖励卡片
│  │  ┌──┐                   │   │
│  │  │图│  一双 Nike 跑鞋    │   │
│  │  │片│  预估 ¥899         │   │
│  │  └──┘                   │   │
│  └─────────────────────────┘   │
│                                 │
│  📊 推进记录                     │  ← 目标日志
│  ├ 07/30 进度 55% → 60%        │
│  ├ 07/15 进度 45% → 55%        │
│  └ 07/01 创建目标               │
│                                 │
└─────────────────────────────────┘
```

#### 7.6.3 通用记录编辑页

```
┌─────────────────────────────────┐
│  ← 取消              保存 ✓     │
├─────────────────────────────────┤
│                                 │
│  [经验] [心得] [反思]            │  ← 类型选择(Chip)
│                                 │
│  标题（选填）_________________   │
│                                 │
│  正文内容_____________________   │
│  ___________________________    │
│  ___________________________    │
│  ___________________________    │
│                                 │
│  B  I  • 引用 — ──              │  ← 富文本工具栏
│                                 │
│  🏷 标签：[沟通] [项目管理] +    │  ← 标签
│  😊 心情：😊 😐 😤 😢 🤔       │  ← 心情选择
│                                 │
│  📷 添加图片                     │
│                                 │
└─────────────────────────────────┘
```

### 7.7 交互动效

| 场景 | 动效 |
|------|------|
| 页面切换 | 左右滑入/滑出（300ms ease-out） |
| 卡片点击 | 缩放反馈（scale 0.98, 100ms） |
| 列表加载 | 渐入 + 上移动画（staggered） |
| 保存成功 | 顶部短暂 toast 提示 + 勾选动画 |
| 目标完成 | 全屏彩带/烟花庆祝动画（lottie 实现） |
| 进度更新 | 进度条平滑增长动画 |
| 下拉刷新 | Material 标准刷新动画 |

### 7.8 图标系统

- 使用 Material Symbols Rounded 图标库
- 底部 Tab 图标：填充式（选中）/ 轮廓式（未选中）
- 功能图标尺寸：24dp（标准）/ 20dp（紧凑）
- 模块图标配合模块专属色使用

---

## 8. 技术架构设计

### 8.1 技术栈总览

| 层面 | 技术选型 | 说明 |
|------|----------|------|
| 框架 | Flutter 3.x (Dart 3.x) | 跨平台 UI 框架 |
| 状态管理 | Riverpod 2.x | 类型安全、可测试、支持代码生成 |
| 路由 | go_router | 声明式路由，支持深链接 |
| 本地数据库 | sqflite + sqflite_sqlcipher | 加密 SQLite |
| 键值存储 | flutter_secure_storage | 敏感配置安全存储 |
| 偏好设置 | shared_preferences | 主题、语言等非敏感配置 |
| 生物识别 | local_auth | 指纹/Face ID 应用锁 |
| 微信 SDK | fluwx | 微信登录 |
| 图片选择 | image_picker | 相册/拍照 |
| 图片缓存 | cached_network_image / 本地文件 | 图片管理 |
| 富文本 | flutter_quill / super_editor | 富文本编辑器 |
| 动画 | lottie | 庆祝动画等复杂动画 |
| 图表 | fl_chart | 统计图表 |
| 日期处理 | intl | 日期格式化、国际化 |
| 本地通知 | flutter_local_notifications | 目标截止提醒 |
| 文件压缩 | archive | 备份文件打包 |
| 加密 | encrypt / pointycastle | 数据加密 |

### 8.2 项目目录结构

```
buildself/
├── lib/
│   ├── main.dart                    # 应用入口
│   ├── app.dart                     # MaterialApp 配置
│   │
│   ├── core/                        # 核心层
│   │   ├── constants/               # 常量定义
│   │   │   ├── app_constants.dart
│   │   │   ├── colors.dart
│   │   │   └── strings.dart
│   │   ├── theme/                   # 主题
│   │   │   ├── app_theme.dart
│   │   │   ├── light_theme.dart
│   │   │   └── dark_theme.dart
│   │   ├── router/                  # 路由
│   │   │   ├── app_router.dart
│   │   │   └── routes.dart
│   │   ├── utils/                   # 工具类
│   │   │   ├── date_utils.dart
│   │   │   ├── crypto_utils.dart
│   │   │   └── export_utils.dart
│   │   └── errors/                  # 错误处理
│   │       └── failures.dart
│   │
│   ├── data/                        # 数据层
│   │   ├── database/                # 数据库
│   │   │   ├── database_provider.dart
│   │   │   ├── database_migrations.dart
│   │   │   └── tables.dart
│   │   ├── models/                  # 数据模型
│   │   │   ├── user_model.dart
│   │   │   ├── work_note_model.dart
│   │   │   ├── life_record_model.dart
│   │   │   ├── goal_model.dart
│   │   │   ├── book_model.dart
│   │   │   ├── reading_note_model.dart
│   │   │   └── murmur_model.dart
│   │   ├── repositories/            # 仓库层
│   │   │   ├── auth_repository.dart
│   │   │   ├── work_repository.dart
│   │   │   ├── life_repository.dart
│   │   │   ├── goal_repository.dart
│   │   │   ├── reading_repository.dart
│   │   │   └── murmur_repository.dart
│   │   └── services/                # 服务层
│   │       ├── backup_service.dart
│   │       ├── export_service.dart
│   │       ├── notification_service.dart
│   │       └── secure_service.dart
│   │
│   ├── features/                    # 功能模块层
│   │   ├── auth/                    # 登录
│   │   │   ├── screens/
│   │   │   │   ├── welcome_screen.dart
│   │   │   │   └── login_screen.dart
│   │   │   └── providers/
│   │   │       └── auth_provider.dart
│   │   ├── home/                    # 首页
│   │   │   ├── screens/
│   │   │   │   └── home_screen.dart
│   │   │   └── widgets/
│   │   ├── work/                    # 工作模块
│   │   │   ├── screens/
│   │   │   │   ├── work_list_screen.dart
│   │   │   │   ├── work_detail_screen.dart
│   │   │   │   └── work_edit_screen.dart
│   │   │   ├── providers/
│   │   │   │   └── work_provider.dart
│   │   │   └── widgets/
│   │   ├── life/                    # 生活模块
│   │   ├── goal/                    # 目标管理模块
│   │   │   ├── screens/
│   │   │   │   ├── goal_board_screen.dart
│   │   │   │   ├── goal_detail_screen.dart
│   │   │   │   ├── goal_edit_screen.dart
│   │   │   │   └── achievement_wall_screen.dart
│   │   │   ├── providers/
│   │   │   │   └── goal_provider.dart
│   │   │   └── widgets/
│   │   │       ├── goal_card.dart
│   │   │       ├── reward_card.dart
│   │   │       └── celebration_overlay.dart
│   │   ├── reading/                 # 阅读模块
│   │   ├── murmur/                  # 碎碎念模块
│   │   └── settings/                # 设置
│   │       ├── screens/
│   │       │   ├── settings_screen.dart
│   │       │   ├── backup_screen.dart
│   │       │   ├── theme_screen.dart
│   │       │   └── about_screen.dart
│   │       └── providers/
│   │           └── settings_provider.dart
│   │
│   └── shared/                      # 共享层
│       ├── widgets/                 # 通用组件
│       │   ├── app_button.dart
│       │   ├── app_card.dart
│       │   ├── app_text_field.dart
│       │   ├── tag_chip.dart
│       │   ├── mood_selector.dart
│       │   ├── empty_state.dart
│       │   └── confirmation_dialog.dart
│       └── layouts/
│           └── main_scaffold.dart   # 底部Tab布局
│
├── assets/                          # 资源文件
│   ├── images/                      # 图片资源
│   ├── icons/                       # 自定义图标
│   ├── animations/                  # Lottie 动画文件
│   │   ├── celebration.json
│   │   └── progress_complete.json
│   └── fonts/                       # 字体文件
│       └── Inter/
│
├── test/                            # 测试
│   ├── unit/
│   ├── widget/
│   └── integration/
│
├── android/                         # Android 原生配置
├── ios/                             # iOS 原生配置
└── pubspec.yaml                     # 依赖配置
```

### 8.3 架构分层说明

采用 **Clean Architecture 分层架构**，保证代码可维护性和可测试性：

```
┌─────────────────────────────────────────┐
│              UI 层 (Screens/Widgets)      │  ← 界面展示与交互
├─────────────────────────────────────────┤
│           状态管理层 (Providers)           │  ← Riverpod 状态管理
├─────────────────────────────────────────┤
│          业务逻辑层 (Repositories)         │  ← 数据聚合与业务规则
├─────────────────────────────────────────┤
│            数据层 (Database/Services)     │  ← 数据库操作与系统服务
└─────────────────────────────────────────┘
```

| 原则 | 说明 |
|------|------|
| 单向依赖 | UI → Provider → Repository → Database，不可反向依赖 |
| 接口隔离 | Repository 定义抽象接口，UI 层不直接操作数据库 |
| 关注点分离 | 每个模块独立管理自己的 screens/providers/widgets |
| 可测试性 | Repository 层可 Mock，Provider 层可独立测试 |

### 8.4 关键依赖配置（pubspec.yaml 摘要）

```yaml
name: buildself
description: 个人成长记录应用
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter

  # 状态管理
  flutter_riverpod: ^2.4.0
  riverpod_annotation: ^2.3.0

  # 路由
  go_router: ^12.0.0

  # 数据库
  sqflite: ^2.3.0
  sqflite_sqlcipher: ^2.2.0
  path: ^1.8.0

  # 存储
  shared_preferences: ^2.2.0
  flutter_secure_storage: ^9.0.0

  # 认证与安全
  local_auth: ^2.1.0
  fluwx: ^4.2.0

  # UI 组件
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  flutter_quill: ^8.0.0
  fl_chart: ^0.66.0
  lottie: ^2.7.0

  # 工具
  intl: ^0.18.0
  uuid: ^4.2.0
  archive: ^3.4.0
  encrypt: ^5.0.0
  flutter_local_notifications: ^16.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.0
  riverpod_generator: ^2.3.0
```

---

## 9. 非功能性需求

### 9.1 性能需求

| 指标 | 目标值 |
|------|--------|
| 冷启动时间 | ≤ 2 秒 |
| 页面切换帧率 | ≥ 60 fps |
| 列表滚动帧率 | ≥ 60 fps |
| 记录保存响应 | ≤ 500ms |
| 搜索响应（1万条记录） | ≤ 1 秒 |
| 数据库查询 | 单次查询 ≤ 100ms |
| 内存占用 | ≤ 150MB |
| 安装包大小 | ≤ 30MB（不含图片资源） |

### 9.2 兼容性需求

| 平台 | 最低版本 | 目标版本 |
|------|----------|----------|
| Android | Android 6.0 (API 23) | Android 14 (API 34) |
| iOS | iOS 13.0 | iOS 17 |

### 9.3 可用性需求

| 需求 | 说明 |
|------|------|
| 离线可用 | 所有核心功能完全离线可用，不依赖网络 |
| 无障碍 | 支持系统字体缩放、VoiceOver/TalkBack 语义标注 |
| 国际化 | 首版仅支持简体中文，预留国际化框架 |
| 适配 | 支持手机竖屏；平板/横屏暂不适配（后续迭代） |

### 9.4 数据可靠性

| 需求 | 说明 |
|------|------|
| 数据一致性 | 所有写操作使用数据库事务，保证原子性 |
| 崩溃保护 | 编辑中内容自动保存草稿（每5秒），防止崩溃丢失 |
| 数据库迁移 | 支持 schema 版本管理和自动迁移脚本 |
| 备份提醒 | 每30天未备份时提醒用户 |

---

## 10. 开发计划与里程碑

### 10.1 开发阶段划分

| 阶段 | 周期 | 主要任务 | 交付物 |
|------|------|----------|--------|
| **P0: 项目初始化** | 第1周 | Flutter项目搭建、架构设计、依赖配置、主题系统 | 可运行的空项目骨架 |
| **P1: 数据层** | 第2-3周 | 数据库设计、Model定义、Repository层、数据库迁移 | 数据层完成，可进行CRUD测试 |
| **P2: 登录与框架** | 第4周 | 微信/手机号登录、底部Tab框架、应用锁 | 可登录进入主界面 |
| **P3: 工作模块** | 第5周 | 记录CRUD、列表、标签、搜索、富文本编辑 | 工作模块功能完整 |
| **P4: 生活模块** | 第6周 | 记录CRUD、图片管理、相册视图、往日今日 | 生活模块功能完整 |
| **P5: 目标管理模块** | 第7-8周 | 目标CRUD、进度管理、奖励系统、庆祝动画、成就墙 | 目标模块功能完整 |
| **P6: 阅读模块** | 第9周 | 书籍管理、笔记CRUD、笔记类型筛选、阅读统计 | 阅读模块功能完整 |
| **P7: 碎碎念模块** | 第10周 | 快速记录、时间线、心情标记 | 碎碎念模块功能完整 |
| **P8: 首页与全局** | 第11周 | 首页整合、全局搜索、通知提醒 | 首页与全局功能完成 |
| **P9: 数据安全与备份** | 第12周 | 数据库加密、备份导出、恢复功能 | 安全体系完成 |
| **P10: 优化与测试** | 第13-14周 | UI打磨、性能优化、单元/集成测试、Bug修复 | 内测版本 |
| **P11: 上架发布** | 第15-16周 | 双端打包、商店审核、上架 | 正式版上线 |

### 10.2 里程碑节点

```
M1 (第3周末) ─── 数据层就绪
       │
M2 (第4周末) ─── 登录流程跑通
       │
M3 (第8周末) ─── 三大核心模块完成（工作/生活/目标）
       │
M4 (第10周末) ─ 全部五大模块完成
       │
M5 (第12周末) ─ 安全与备份完成
       │
M6 (第14周末) ─ 内测版本就绪
       │
M7 (第16周末) ─ 正式上架
```

### 10.3 开发资源估算

| 角色 | 人数 | 说明 |
|------|------|------|
| Flutter 开发 | 1-2人 | 全栈开发，负责 UI + 逻辑 + 数据层 |
| UI 设计 | 1人 | 负责视觉设计、图标、动效（可兼职） |
| 测试 | 1人 | 可由开发兼任 |

> 若为个人独立开发，建议优先实现核心功能（工作/目标/阅读三模块），生活与碎碎念模块可延后迭代。

---

## 11. 打包发布与上架策略

### 11.1 Android 打包与发布

#### 11.1.1 签名配置

| 步骤 | 说明 |
|------|------|
| 生成签名密钥 | `keytool -genkey -v -keystore buildself.jks -keyalg RSA -keysize 2048 -validity 10000 -alias buildself` |
| 配置 key.properties | 在 `android/` 目录下创建密钥配置文件 |
| 配置 build.gradle | 在 `android/app/build.gradle` 中配置签名信息 |
| 构建_release APK/AAB | `flutter build appbundle --release` 生成 `.aab` 文件 |

#### 11.1.2 发布渠道

| 渠道 | 说明 | 优先级 |
|------|------|--------|
| Google Play | 国际Android市场，需开发者账号（$25一次性） | 中（国内用户少） |
| 国内应用市场 | 华为、小米、OPPO、vivo、应用宝等 | 高 |
| 官网直装 | 提供 APK 直接下载（适合小范围分享） | 高 |

> **国内应用市场注意事项**：
> - 华为、小米等主流市场需要软件著作权（可申请或代办）
> - 部分市场要求 APP 备案（2023年起工信部要求）
> - 审核周期 3-7 个工作日
> - 可先通过官网/蒲公英等平台提供内测下载

#### 11.1.3 应用图标与资源

| 资源 | 规格 |
|------|------|
| 应用图标 | 512x512px (Play Store) + 多分辨率 mipmap |
| 适配图标 | Android 13+ 支持 Themed Icon (单色自适应图标) |
| 启动页 | 1080x1920px + 品牌Logo |

### 11.2 iOS 打包与发布

#### 11.2.1 证书与配置

| 步骤 | 说明 |
|------|------|
| Apple Developer 账号 | 个人/公司账号（$99/年） |
| 创建 App ID | 在 Apple Developer Portal 创建 Bundle ID（如 `com.buildself.app`） |
| 创建证书 | Development Certificate（开发）+ Distribution Certificate（发布） |
| 创建 Provisioning Profile | 关联 App ID 和证书 |
| 配置 Xcode | 在 `ios/Runner.xcworkspace` 中配置签名和能力 |

#### 11.2.2 发布流程

| 步骤 | 说明 |
|------|------|
| 构建 | `flutter build ipa --release` 生成 `.ipa` 文件 |
| 上传 | 使用 Xcode → Organizer → Distribute App → App Store Connect |
| App Store Connect | 创建 App 记录，填写元数据、截图、描述、隐私政策 |
| 审核 | 提交审核，审核周期 1-3 天（首次可能更长） |
| 上架 | 审核通过后发布上线 |

#### 11.2.3 iOS 特别注意事项

| 事项 | 说明 |
|------|------|
| 隐私政策 | App Store 要求提供隐私政策 URL，即使纯本地存储也需声明 |
| 权限说明 | 相册权限、相机权限、Face ID 权限需在 Info.plist 填写用途说明 |
| 微信登录 | 需在微信开放平台配置 iOS Universal Link |
| 审核风险 | 纯本地存储无数据收集，审核相对容易通过 |
| 截图资源 | 需提供 6.7寸(iPhone 15 Pro Max) 和 6.5寸 截图 |

### 11.3 发布检查清单

| 检查项 | Android | iOS |
|--------|---------|-----|
| 应用图标和启动页 | ✅ | ✅ |
| 版本号和构建号 | ✅ | ✅ |
| 签名配置 | ✅ | ✅ |
| 权限声明 | ✅ | ✅ |
| 隐私政策 | ✅ | ✅ |
| 应用描述和关键词 | ✅ | ✅ |
| 截图和预览视频 | ✅ | ✅ |
| 混淆和优化 | `--obfuscate --split-debug-info` | Release mode |
| 数据备份测试 | ✅ | ✅ |
| 崩溃测试 | ✅ | ✅ |

---

## 12. 后续迭代规划

### 12.1 v1.x 迭代方向

| 版本 | 功能 | 优先级 |
|------|------|--------|
| v1.1 | 数据统计仪表盘（记录趋势、目标达成率、阅读统计） | 高 |
| v1.2 | 桌面小组件（Android Widget / iOS Widget）快速记录 | 中 |
| v1.3 | 主题自定义（更多配色方案、自定义主题色） | 中 |
| v1.4 | iPad / 平板适配 | 中 |
| v1.5 | AI 辅助写作（本地小模型或可选接入） | 低 |

### 12.2 v2.0 远期规划

| 功能 | 说明 |
|------|------|
| 云端同步 | 补建后端服务，实现多设备数据同步 |
| 数据分析 | 年度成长报告、智能复盘建议 |
| 导出多样化 | 支持 PDF 成长报告、EPUB 电子书格式导出 |
| 社区功能 | 可选的匿名成长社区，分享目标和读书笔记 |

---

## 13. 附录

### 13.1 术语表

| 术语 | 说明 |
|------|------|
| PRD | Product Requirements Document，产品需求文档 |
| CRUD | Create, Read, Update, Delete，增删改查 |
| SDK | Software Development Kit，软件开发工具包 |
| AAB | Android App Bundle，Android 应用发布包格式 |
| IPA | iOS App Store Package，iOS 应用包格式 |
| SQLCipher | SQLite 数据库加密扩展 |
| BaaS | Backend as a Service，后端即服务 |
| Material You | Google 的最新设计语言，支持动态取色 |

### 13.2 参考资源

| 资源 | 链接 |
|------|------|
| Flutter 官方文档 | https://docs.flutter.dev |
| Riverpod 文档 | https://riverpod.dev |
| sqflite_sqlcipher | https://pub.dev/packages/sqflite_sqlcipher |
| fluwx 微信SDK | https://pub.dev/packages/fluwx |
| Material Design 3 | https://m3.material.io |
| Apple App Store 审核 | https://developer.apple.com/app-store/review/guidelines |
| Google Play 发布 | https://developer.android.com/distribute |

### 13.3 修订记录

| 日期 | 版本 | 修订内容 | 修订人 |
|------|------|----------|--------|
| 2026-07-30 | v1.0 | 初始版本创建 | Buildself Team |

---

*本文档为 Buildself APP 的产品需求文档，旨在为开发团队提供完整的设计蓝图和技术参考。文档将随产品迭代持续更新。*
