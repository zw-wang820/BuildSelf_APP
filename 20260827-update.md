# BuildSelf 开发日志 — 2026-08-27

> 个人成长 App（Flutter · Android/iOS 双端 · 本地优先）
> 今日 6 次提交，覆盖：书籍编辑 + 封面改造、书架统计卡点击筛选、书籍阅读统计弹层、阅读指南（方法论）。

---

## 一、提交记录

| Commit | 说明 | 规模 |
| --- | --- | --- |
| `735a68e` | 书籍编辑（添加/编辑双模式表单）+ 封面改造（渐变+书名+每书独立 emoji） | 12 文件 +324/-51 |
| `8ec3f4c` | docs: 2026-08-27 开发日志 | 1 文件 +42 |
| `175a6ef` | 书架统计卡点击切换对应筛选（选中态联动） | 1 文件 +43/-28 |
| `79dc2f1` | 书籍阅读统计弹层 — 四类笔记计数/占比/近7天新增/点击下钻 Tab | 2 文件 +407 |
| `b897024` | docs: 开发日志补充 | 1 文件 +13/-1 |
| `0f50566` | 阅读指南 — 选书/阅读/落地方法论 + 行动引导（选书→新建改变笔记） | 8 文件 +813/-2 |

当前分支 `feature/ui`，与 `origin/feature/ui` 完全同步，工作区干净。

---

## 二、模块改动明细

### 1. 书籍编辑功能（新增）
- **背景**：此前书籍添加后无法再次编辑（详情页仅删除/新建笔记），如补录作者无从下手。
- **入口**：书籍详情页右上角菜单新增「编辑」。
- **表单**：`BookAddScreen` 改造为添加/编辑双模式（`Book? book` 参数）：
  - 编辑模式预填书名/作者/状态/封面图标，保存走 `updateBook`
  - 状态联动：改为「在读」自动补开始日期（首次）、「已读完」自动补完成日期（首次）
- 路由 `AppRoutes.bookAdd` 支持透传 `Book?` 参数，返回后详情页自动刷新。

### 2. 书架封面改造（渐变底 + 书名 + 每书独立 emoji）
- **方案**（用户拍板）：渐变底色 + 封面书名文字 + 不同书籍不同 emoji；不采用颜色自选与相册真实图片。
- **数据层 v8 迁移**：`books` 表新增 `cover_emoji` 列（null = 按书名自动分配），存量数据自动兼容。
- **封面工具**（新增 `reading_cover.dart`）：16 个预设 emoji + `resolveCoverEmoji`（手动指定优先，否则按书名 hash 稳定分配，同一本书处处一致）。
- **表单**：封面实时预览（渐变 + emoji + 书名随输入变化）；emoji 选择器（再点取消回自动）。
- **封面 UI**：书架网格卡、最近阅读卡、书籍详情页、首页「正在阅读」行统一改为渐变 + emoji + 书名；首页行渐变改用书籍自身 `coverColor`（此前按列表序号轮询，翻页会变色）。

### 3. 文案
- `strings.dart` 新增「编辑书籍 / 封面图标 / 不选择则按书名自动匹配」三语言（简/繁/英）。

### 3. 书架统计卡点击筛选（新增）
- 顶部四张统计卡（总书籍/已读完/正在读/想读）此前为纯展示，无交互。
- 点击卡片 = 切换书架对应筛选（总书籍→全部、已读完→已读完、正在读→在读、想读→想读），卡片选中态高亮（边框 + 背景加深），与下方筛选标签联动，180ms 过渡。

### 4. 书籍阅读统计弹层（新增）
- **方案**（用户审核通过）：详情页信息卡右侧「📊 统计」→ 底部弹层；含「近 7 天新增」；4 类行点击下钻对应笔记 Tab。交互说明见《书籍阅读统计-交互说明.md》。
- **面板内容**：拖拽条 + 标题（书名）+ 笔记总数 + 4 行统计（📝摘抄/💡心得/🤔思考/🔄改变，条数 + 占比进度条 + 百分比）+ 🆕 近 7 天新增 + 空状态引导。
- **实现**：复用 `getNotesByBook` 一次取全量、内存分组计数，无新表无新接口；行点击 `pop(index)` → `_tabController.animateTo`（全部0/摘抄1/心得2/思考3/改变4）；颜色沿用场景色（紫/蓝/橙/绿）。

### 5. 阅读指南（方法论内容页，新增）
- **理念**（用户提出）：阅读的目的是改变——不行动不改变，阅读就失去大部分意义。把「如何选书→如何阅读→如何落地」的方法论内置到 App。
- **方案**（用户确认）：方案 B（列表+详情）；❓ 入口；内容/语气 OK；行动引导按钮（选书→新建「改变」笔记，真闭环）。
- **入口**：书架 AppBar ❓（在 🔍 左侧）→ 阅读指南。
- **内容**（数据驱动，本地静态）：📖 为什么阅读（读→思→行→变 闭环）· 🎯 如何选书（四步筛选 + 三问测试）· 📚 如何阅读（四层次 + SQ3R + 主动四问）· 🚀 如何落地（RIA 便签法 + 费曼输出 + 三色标注）。
- **行动引导**：「把这本书的方法变成行动」→ 选书 bottom sheet（封面+书名+作者）→ 新建笔记页默认选中「改变」类型（`NoteEditArgs` 新增 `initialType`）。
- **结构**：新增 `guide/` 目录 3 文件（guide_articles.dart 数据模型+内容、列表页、详情页）+ 路由 2 条 + 书架入口 + NoteEditArgs 扩展。资料出处见《阅读指南-资料与方案.md》。

---

## 三、遗留事项

- `app-release.apk` 仍为 7-30 构建，未更新。
