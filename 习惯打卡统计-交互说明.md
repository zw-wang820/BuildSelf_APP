# 习惯打卡统计 - 交互说明

> 版本：v0.1（待审核）  
> 范围：查看**某个习惯**的打卡记录，以**月历**形式展示，支持月份切换与日期补卡/取消  
> 前置：习惯打卡模块已上线（打卡/补打/连续天数/列表统计概览）

---

## 一、需求理解

用户想对**单个习惯**查看打卡历史：这个习惯哪些天打了卡、连续坚持了多久，用**日历**直观呈现。同时日历可反哺打卡操作（漏卡的日子直接补上）。

现有数据完全支撑：`habit_logs` 表已存每天打卡记录（habit_id + date），无需新增表。

---

## 二、推荐方案（A：习惯卡片 📊 入口 + 日历详情页）

### 入口

习惯列表页 → 每个习惯卡片**新增「📊」入口按钮**（位置：卡片右侧、今日打卡圈左侧）→ 进入该习惯的「打卡日历」页。

```
┌────────────────────────────────────────┐
│ 习惯打卡                        (AppBar)│
│ ┌────────────────────────────────────┐ │
│ │ ⭕图标  习惯名称        📊  ⭕今日打卡│ │  ← 卡片新增 📊 按钮
│ │         🔥 已连续 5 天 · 今天待打卡   │ │
│ └────────────────────────────────────┘ │
│                （其他习惯卡片…）          │
└────────────────────────────────────────┘
            ▲ 点 📊
┌────────────────────────────────────────┐
│ ← 💪 健身                             │
│ ┌────────────────────────────────────┐ │
│ │ 本月 12 天 │ 连续 5 天 │ 累计 45 天  │ │  ← 统计摘要三卡
│ └────────────────────────────────────┘ │
│        ‹  2026年8月  ›                 │  ← 月份切换
│   一 二 三 四 五 六 日                  │
│   ┌──┬──┬──┬──┬──┬──┬──┐              │
│   │  │  │  │  │● │● │● │              │  ← 打卡日 = 习惯色
│   │● │  │● │● │  │● │● │              │     今天 = 边框高亮
│   │  │● │  │● │  │● │● │              │     可点日期 = 补卡/取消
│   │● │● │● │  │  │  │  │              │
│   └──┴──┴──┴──┴──┴──┴──┘              │
│  图例：● 已打卡   点击日期可补卡/取消    │
└────────────────────────────────────────┘
```

### 日历页交互细节

| 环节   | 行为                                                                                 |
| ---- | ---------------------------------------------------------------------------------- |
| 打开   | 点卡片 📊 → 日历页（AppBar = 习惯图标 + 名称），默认定位**当前月**                                       |
| 统计摘要 | 三张卡：**本月打卡 X 天** / **连续打卡 Y 天**（沿用现有 streak 逻辑）/ **累计打卡 Z 天**（历史总次数）               |
| 月份切换 | 日历头部「‹ 2026年8月 ›」，左右箭头切换月份；支持**无限前翻**（查看历史），**后翻不超当前月**                            |
| 月历渲染 | 自绘 7 列网格（周一~周日表头），月份内每天一格：已打卡 = 习惯色圆底 + 白色对勾；未打卡 = 透明；今天 = 主题色描边                   |
| 日期点击 | 点击**已打卡**日期 → 弹确认「取消这天打卡？」→ 取消后色块消失；点击**未打卡且 ≤ 今天**日期 → 直接补卡（色块出现）；**未来日期不可点**（灰色） |
| 数据刷新 | 补卡/取消后：日历即时更新 + 统计摘要同步 + 返回列表页后列表数据刷新                                              |
| 空习惯  | 习惯创建后无打卡记录：日历全空，正常展示（不特殊处理）                                                        |

### 为什么自绘月历（不引入第三方库）

- 项目现有风格为**零第三方依赖**（待办统计柱状图也是纯 widget 自绘）
- 月历网格结构简单（7 列 GridView + 月份计算），自绘约 100 行，可控可调
- 引入 `table_calendar` 等库需联网拉包，且样式与现有 UI 体系难统一

---

## 三、备选方案对比

| 方案                   | 入口/形态              | 优点               | 缺点                   |
| -------------------- | ------------------ | ---------------- | -------------------- |
| **A 卡片📊 + 日历页**（推荐） | 卡片新增 📊 按钮 → 独立日历页 | 入口显眼、日历空间大、可补卡闭环 | 多一层页面                |
| B 卡片点击进详情            | 整卡可点击 → 日历页        | 无需新按钮            | 卡片已有打卡圈/补卡交互，整卡点击易误触 |
| C 列表内嵌展开             | 点卡片展开月历            | 零跳转              | 多卡片同时展开布局复杂，空间受限     |

---

## 四、数据与实现要点

1. **无迁移**：复用 `habit_logs`（habit_id + date yyyy-MM-dd）。
2. **新增查询**：`HabitRepository.getLogsByHabitId(String habitId)` 返回 `Set<String>`（现有 `getLogsByHabit` 是全量 map，单查询更精准轻量）。
3. **月历算法**：定位月份第一天 `DateTime(year, month, 1)` → 用 `weekday` 计算首日偏移（周一为首列）→ 当月天数 → 渲染 42 格或 35 格（动态行数）。
4. **补卡/取消**：复用 `toggle(habitId, date)`（已支持任意日期打卡/取消，返回本次是否打卡成功）。
5. **UI 复用**：习惯色 `kHabitPalette[colorIndex]`；统计卡样式对齐现有习惯列表顶部概览；日期格用习惯色圆点 + 对勾，深/浅色主题自适应。
6. **改动文件**：新增 `habit/screens/habit_stats_screen.dart`；`habit_item_card.dart` 加 📊 按钮回调；`habit_list_screen.dart` 传入入口并刷新返回数据；`routes.dart`/`app_router.dart` 注册 `/habit/stats`（传 habitId）；`habit_repository.dart` 加单查询。

---

## 五、验证清单（真机）

1. 习惯卡片出现 📊 按钮，点击进入该习惯的日历页
2. 默认定位当前月；本月已打卡日期有色块，与列表打卡状态一致
3. 左右切换月份，历史月份正常展示；后翻不超当前月
4. 点击某已打卡日期 → 弹确认 → 取消后色块消失、统计数 -1
5. 点击某未打卡的历史日期 → 直接补卡、色块出现、统计数 +1
6. 未来日期不可点击
7. 统计三卡（本月/连续/累计）数值正确
8. 返回列表页，卡片状态与今日打卡圈已同步

---

## 六、待确认

- [x] **方案 A（卡片 📊 入口）** 是否采纳？
- [x] 日历上是否支持**点日期直接补卡/取消**？（推荐支持，形成闭环；若担心误触，可改为仅查看）
- [x] 统计摘要三卡：本月打卡/连续打卡/累计打卡，是否有增减？
