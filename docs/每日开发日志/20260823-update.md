# BuildSelf 开发日志 — 2026-08-23

> 个人成长 App（Flutter · Android/iOS 双端 · 本地优先）
> 今日共 5 次提交，覆盖：成长日报首页、四大模块交互、习惯打卡、emoji 图标体系、待办能力增强。

---

## 一、提交记录

| Commit | 说明 | 规模 |
| --- | --- | --- |
| `e3fa031` | 成长日报首页重设计 + 待办/目标/阅读交互模块 + 全站主题自适应 | 多文件 |
| `1cabe1b` | 习惯打卡模块（v4 迁移）+ 首页精简（去 hero / 数据条去图标） | 12 文件 +1310/-179 |
| `aed5518` | 全站图标替换为 emoji 体系 + 图标居中修复 | 33 文件 +248/-186 |
| `2d6af99` | 待办逻辑修正：完成项删除线展示 + 列表页「全部」含已完成 | 2 文件 +18/-12 |
| `53f6ccd` | 待办周期重复（v5 迁移）+ 编辑功能 + 左滑删除 | 9 文件 +912/-87 |

当前分支 `feature/ui`，历史连续，工作区干净。

---

## 二、模块改动明细

### 1. 首页 · 成长日报
- 顶部问候 + 日期（含周几）+ slogan
- **数据条三卡**：待办 / 进行中目标 / 习惯打卡（习惯显示「今日已打卡/总数」），去掉图标只留数字 + 名称
- 快捷记录入口（💼工作 / 🌿生活 / 📖阅读 / ✨碎碎念）+ 四大区块
- **今日待办**：未完成优先 4 条 + 已完成补位 2 条，**完成项保留并以删除线展示**，点击行可编辑，勾选可完成/恢复
- 进行中的目标（最多 3 条）、正在阅读（最多 3 本）、今日复盘（空态占位）
- 去除「今日成长指数」hero 模块（按需求精简）
- 区块入口返回后自动刷新对应数据

### 2. 待办模块（能力最完整）
- 新建表单：内容 / 备注 / 分类（工作·生活·阅读·学习）/ 优先级 / 截止（今天·明天·后天·自定义）
- **周期重复（时间驱动）**：每天 / 每周（周一~周日多选）/ 每月 / 每年；可设间隔（每 N 周期）与终止条件（永不结束 / 按次数 / 按日期）
  - 到周期自动出现新实例（打开 App / 进入列表时惰性补建），错过多期只补最近一期
  - 卡片显示 `🔄 每周五` 重复徽章
- **编辑**：点击卡片弹出编辑表单，全部字段（含重复规则）可修改
- **左滑删除**：滑出红色 🗑️ 背景，滑到底物理删除 + Toast
- 筛选：全部（含已完成）/ 工作 / 生活 / 阅读 / 已完成
- 已完成勾选 = ✅ emoji 弹簧动画；逾期红字提示

### 3. 目标模块
- 列表页：统计概览（短/中/长/已完成）+ 筛选标签 + 目标卡片（类型徽章 / 截止 / 🎁奖励 / 进度条入场动画 / 里程碑✓）
- 新建表单：名称 / 类型（⚡短期·🎯中期·🌟长期）/ 截止（1周·1月·3月·自定义）/ 奖励 / 里程碑动态增删 / 备注
- 创建后置顶 + 进度 5% 起步动画 + Toast
- 详情页：推进记录、奖励卡、编辑/更多操作；成就徽章墙（15 枚 emoji 徽章）

### 4. 阅读模块
- 书架：统计（总书籍/已读完/正在读/想读）+ 状态筛选 + 3 列网格封面（渐变 + 进度条 + 状态徽章）+ 最近阅读
- 新建书籍表单：实时封面预览、状态切换（📖想读/👀在读/✅已读完）、当前页/总页数、8 色渐变封面
- 书籍详情：阅读进度、读书笔记（按类型）、添加笔记

### 5. 习惯打卡模块（本次新增）
- 数据层 v4 迁移：`habits` + `habit_logs` 表
- 列表页：统计 4 卡（总习惯/今日完成/连续最长/本月打卡）+ 筛选（全部/已完成/未完成）
- 打卡圈（复用待办弹簧复选框）+ 连续天数（今天没打从昨天起算不断签）+ **补昨天**
- **庆祝动效**：打卡成功触发全屏粒子 burst + 🔥 火焰浮动（1.6s 自动消失）
- 新建习惯表单：名称 + 12 个 emoji 图标 + 8 色板

### 6. 全站 emoji 图标体系（按《图标.txt》规范）
- 新增 `EmojiIcon` 组件（Text 渲染 emoji + 固定占位框 + 居中缩放）
- 底栏 5 Tab：🏠首页 / 💼工作 / 🌿生活 / 🎯目标 / 📖阅读（选中放大 + 场景色圆底）
- 全站 90+ 处 Material 图标 → emoji：🔍搜索 ➕添加 ✏️编辑 🗑️删除 ❌关闭 ✅完成 ⏰截止 ↺恢复 📥导出 🎁奖励 📝笔记 📊统计 ⏰等
- 统计概览小图标、Toast 内部图标、系统图标（锁/微信/位置/主题）刻意保留 Material

### 7. 全局主题自适应
- 全部页面文字颜色改为主题自适应（浅色深字 / 深色浅字），修复浅色模式下文字发灰问题

---

## 三、数据库迁移（自动升级，存量用户无感）

| 版本 | 变更 |
| --- | --- |
| v2 → v3 | 书籍表新增阅读进度 / 封面色 / 最近阅读字段 |
| v3 → v4 | 新增习惯打卡表 `habits` / `habit_logs` |
| v4 → v5 | 待办表新增重复规则字段（type / interval / weekdays / max_count / end_date / origin_id）+ 索引 |

---

## 四、质量校验

- `dart analyze lib` → **0 error / 6 warning（均为改动前基线，本次所有改动 0 新增告警）**
- 交互遵循「先文档确认 → 实现 → 真机测试 → 提交」节奏，各功能均已由用户验证通过

---

## 五、遗留事项 / 后续建议

- 首页「今日复盘」仍为空态占位（数据模型未建）
- 目标/阅读的统计概览小图标仍是 Material（emoji 规范未覆盖短/中/长/成就语义）
- 习惯打卡暂无周视图/历史日历；番茄钟模块尚未实现
- 待办删除为物理删除（未接入回收站），如需可后续调整
