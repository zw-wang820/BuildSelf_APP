# 习惯编辑 + 总览统计 - 交互说明

> 版本：v0.1（待审核）
> 范围：① 习惯支持再次编辑（名称/图标/颜色）；② 习惯**总览**统计页（当前仅有单习惯日历统计）
> 前置：习惯打卡已上线（新建/打卡/补卡/日历统计含农历）

---

## 一、需求理解

- **习惯编辑**：习惯创建后目前无法修改。用户想改名称、换图标、换颜色。
- **总览统计**：现有统计是"单习惯"维度（点某习惯的 📊 进日历页）。需要**所有习惯的聚合统计**，看清全局打卡情况。

---

## 二、习惯编辑方案

### 入口（推荐 A：卡片 ⋯ 菜单）

| 方案 | 形态 | 优点 | 缺点 |
|---|---|---|---|
| **A 卡片右上角「⋯」菜单**（推荐） | 卡片新增 ⋯ 按钮 → 弹出菜单（编辑 / 删除） | 显式、不误触、可顺带删除 | 卡片多一个按钮 |
| B 长按卡片 | 长按弹菜单 | 无新按钮 | 发现性差（用户不知道可长按） |
| C 卡片点击进编辑 | 点卡片直接进编辑表单 | 最直接 | 与打卡圈/📊 按钮并存易误触 |

### 编辑表单

- 复用现有新建表单 `add_habit_sheet` 改造为**新建/编辑双模式**（传 `Habit?`）：
  - 编辑模式：标题「编辑习惯」，预填 名称/图标/颜色，底部按钮「保存修改」
  - 字段：名称（≤20 字）、图标（12 个 emoji 选择）、颜色（8 色板）——与新建完全一致
- 保存：`HabitRepository.update(habit)`（新方法，更新 name/icon/color_index）
- 保存后：列表页刷新（图标/颜色/名称立即生效）；日历页等无影响（统计与名称无关）

### 是否顺带「删除习惯」？（待决策）

编辑入口通常伴随删除，建议「⋯」菜单同时提供：
- 删除 → 二次确认弹窗（提示"将同时删除该习惯的全部打卡记录"）→ 物理删除习惯 + 级联删除打卡记录（`habit_logs` 表已有 `ON DELETE CASCADE`）
- 删除后列表刷新

---

## 三、总览统计方案（推荐 A：独立统计页）

### 入口
习惯列表页 **AppBar 右侧新增「📊」按钮** → 总览统计页（与单习惯卡片上的 📊 图标相同但位置不同，语义为"全部习惯"）。

### 页面内容

```
┌────────────────────────────────────┐
│ ← 习惯总览                        │
│ ┌────────────────────────────────┐ │
│ │ 总习惯 8 · 今日完成 5 · 连续最长 12│ │  ← 全局摘要（3-4 卡）
│ │ 本月打卡 15 天 · 累计打卡 203 次  │ │
│ └────────────────────────────────┘ │
│ 近 7 天打卡趋势（自绘柱状图）         │
│  ▁▂▃▃▂▄▅ 5 6 7 8 9 10 11          │
│                                    │
│ 各习惯统计（排行列表）               │
│ ┌────────────────────────────────┐ │
│ │ 💪 健身    本月12·连续5·累计45  › │ │
│ │ 📚 阅读    本月10·连续3·累计30  › │ │
│ │ 🏃 跑步    本月 8·连续2·累计12  › │ │
│ │   （点击 → 进入该习惯的日历统计页） │ │
│ └────────────────────────────────┘ │
└────────────────────────────────────┘
```

### 交互细节

| 环节 | 行为 |
|---|---|
| 打开 | 列表页 AppBar 📊 → 总览统计页，默认定位当前月 |
| 全局摘要 | 总习惯数 / 今日完成 / 连续最长（现有列表页顶部 4 卡升级版）+ **本月打卡天数**（所有习惯合计、按日去重）/ **累计打卡总次数** |
| 近 7 天趋势 | 自绘柱状图（每日打卡习惯数），复用待办统计页的纯 widget 实现风格，零依赖 |
| 各习惯排行 | 每行：图标 + 名称 + 本月打卡 X 天 · 连续 Y 天 · 累计 Z 次，按本月打卡天数倒序 |
| 下钻 | 点某习惯行 → 进入该习惯的**日历统计页**（现有功能，闭环） |
| 刷新 | 每次进入重新取数；从子页返回后刷新 |

### 备选对比

| 方案 | 形态 | 优点 | 缺点 |
|---|---|---|---|
| **A 独立统计页**（推荐） | AppBar 📊 → 新页 | 空间足、可扩展（趋势图/排行）、不挤列表 | 多一层页面 |
| B 列表页顶部扩展 | 现有 4 卡升级为 6-8 卡 | 零跳转 | 卡片过多挤压列表，趋势图无处放 |
| C 弹层 | 底部弹出总览 | 轻量 | 趋势图/列表在弹层内受限，滚动体验差 |

---

## 四、数据与实现要点

1. **无数据库迁移**：编辑只动 `habits` 现有列（name/icon/color_index）；统计复用 `habit_logs`。
2. **新增方法**（`habit_repository.dart`）：
   - `update(Habit habit)`：按 id 更新名称/图标/颜色
   - `delete(String habitId)`：删除习惯（habit_logs 级联自动清）
   - 总览统计可直接用现有 `getAll` + `getLogsByHabit` 内存聚合（无需新 SQL）
3. **表单改造**：`add_habit_sheet.dart` 支持 `Habit? habit` 编辑模式；或新建独立编辑表单（推荐改造复用，UI 一致）。
4. **改动文件**：
   - 编辑：`add_habit_sheet.dart`（双模式）+ `habit_item_card.dart`（⋯菜单）+ `habit_list_screen.dart`（处理编辑/删除）+ `habit_repository.dart`（update/delete）
   - 总览：新增 `habit/screens/habit_overview_screen.dart` + 列表页 AppBar 📊 + `routes.dart`/`app_router.dart`（`/habit/overview`）
5. **删除确认**：AlertDialog 二次确认，删除后 Toast。

---

## 五、验证清单（真机）

**编辑**
1. 习惯卡片出现 ⋯ 菜单 → 编辑 → 表单预填当前名称/图标/颜色
2. 改名/换图标/换颜色 → 保存 → 列表卡片立即更新（名称/图标/色）
3. 取消编辑不产生变化
4. （若含删除）删除 → 二次确认 → 习惯消失、打卡记录同步清除

**总览统计**
5. 列表页 AppBar 📊 → 总览页，摘要卡数值与列表页顶部一致
6. 近 7 天柱状图与每日实际打卡数一致
7. 排行列表按本月打卡倒序，数值正确
8. 点某习惯行 → 进入其日历统计页 → 返回后总览刷新

---

## 六、待确认

- [ ] **编辑入口**：卡片「⋯」菜单（推荐）还是其他？
- [ ] 是否**顺带加删除习惯**功能？（推荐加，形成管理闭环）
- [ ] **总览统计页**：方案 A 独立页（推荐）还是 B/C？
- [ ] 总览摘要字段：总习惯/今日完成/连续最长/本月打卡/累计次数，是否有增减？
- [ ] 是否需要「近 7 天趋势柱状图」？（推荐加，直观）
